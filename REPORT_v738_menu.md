# v738 — ToriTonで「丼数」表示／出数が変わらない：原因と修正

## 1. スクリーンショットが示している核心

診断画面のこの行が答えです。

```
実データの範囲: 2026-07-01 〜 2026-07-01（1日分）
取得行数 103 ／ 商品数 103 ／ 出数合計 524
```

**クラウド（toast_item_sales）に、ToriTonの7月は「7/1の1日分」しか入っていません。**

端末の「同期」は**クラウドを読むだけ**なので、元が1日分しかなければ何度押しても
出数は524のまま変わりません。端末側の不具合ではなく、**取り込み元が足りていない**状態です。

同月の客数 2,101 に対して出数合計 524（1件あたり0.25）という警告も、これを裏付けています。

### 直し方
診断画面の一番下「**31日分を再取り込み（Toastから取り直す）**」を実行してください。
これは Toast に接続して31日分を取得し、クラウドへ入れ直します（30〜60秒）。
その後にメニュータブの出数が正しくなります。

**「取り込み直す」ボタンでは直りません**（こちらはクラウドを読み直すだけです）。
紛らわしかったので、v738で両者の違いを画面に明記しました。

## 2. 「丼数」表示（ToriTonなのにTotoyaの言い方）

診断の文言が **全店共通で「客数/丼数」と固定** されていました。
店舗ごとの呼び方を返す関数（`guestCountLabel`）はすでにあったのに、ここだけ使われていませんでした。

| 店舗 | 正しい呼び方 |
|---|---|
| ToriTon / Tenkichi / Marujuu | 客数 |
| Totoya 3店 | 丼数 |
| Waikiki FSP / Garlic Shack | Plate |

**修正**：診断の表示を `guestCountLabel(storeId)` に統一。「1客あたり／1丼あたり／1皿あたり」の
単位も店舗ごとに切り替わります。

## 3. 「店舗ごとの設定になっていますか？」→ なっています

出数の取り込みは**店舗のToast GUID単位**です。

```
mode:'cronSync', days:31, restaurantGuid: st.toastGuid   ← 店舗ごと
```

夜間の自動取り込みも店舗ごとに動きます。今回は**表示ラベルだけが全店共通**でした。

ただし1点、再取り込み後の読み直しが**常に「今月」**になっていたため、
別の月を表示しているときに反映されませんでした。**表示中の月を読み直す**よう修正しています。

## 4. 修正内容（v738）
- 診断の呼び方を店舗ごとに（丼数／客数／Plate）
- 「取り込み直す」＝クラウド読み直し、「31日分を再取り込み」＝Toastから取り直し、と画面に明記
- 再取り込みは**表示中の月**を読み直す（従来は常に今月）
- 再取り込み完了後、**診断を自動で開き直す**（結果をその場で確認できる）

## 5. 変更ファイル
- `index.html` : APP_VERSION 737 → **738**
- `sw.js` : SW_BUILD 737 → **738**

DB変更なし。

## 6. テスト
`verify_v738.js` — **15/15 PASS**

- 店舗ごとの呼び方（ToriTon=客数／Totoya=丼数／Waikiki=Plate）
- 固定文言「客数/丼数」の廃止と `guestCountLabel` への統一
- 再取り込みが店舗GUID単位・表示中の月を読み直す・診断を開き直す
- 「同期」と「Toast再取得」の違いの明記

回帰：v737 18/18、v736 22/22、v735 40/40、v734 31/31、v732 予算 70/70、v727 95/95。

## 7. 確認していただきたいこと

1. v738 に更新（更新バナー → SW_BUILD 738）
2. 食材管理 ＞ メニュー ＞ ToriTon / 2026.7 で「**出数の診断**」
3. 一番下の「**31日分を再取り込み（Toastから取り直す）**」を実行（30〜60秒）
4. 自動で開き直る診断で「実データの範囲」が**7/1〜7/26 程度に伸びている**か確認
5. 伸びていれば、メニュータブの出数が正しくなります

## 8. あわせて確認したいこと（サーバ側）

7月が1日分しか入っていないのは、**夜間の自動取り込みが出数まで回っていない可能性**があります。
他店（Tenkichi / Totoya 3店 / Waikiki）でも診断を開き、「実データの範囲」が1〜2日分しか
無いようであれば、cronSync の出数取り込みの設定を見直す必要があります。
どの店舗が何日分あるか教えていただければ、原因を絞り込みます。

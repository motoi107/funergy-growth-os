# Funergy Growth OS — 引き継ぎ v840

**v840：ダッシュボードと管理ダッシュボードを統合した1画面コンソール（PC特化）。**

入口は `dashboard` 1本。G4以上はコンソール、経理・G3・Crew は従来のダッシュボードのまま。

---

## 0. 最初にやること

```bash
unzip funergy_v840_handoff.zip
tar -xJf handoff_v840.tar.xz
cd handoff_v840
TZ=Pacific/Honolulu node run_verify.js
node make_preview.js        # → preview.html。ブラウザで開くと画面が見られる
```

**期待値**

```
合計 PASS=3486  FAIL=0  実行=64/79
合流ルールの未定義: 1        ← spl_skills_st_ の1件だけが既知
同期対象で未保護: 0
予算の対象月: 3 / 7
APP_VERSION=840 / SW_BUILD=840
CRLF=57088 / lone LF=0
```

`TZ=Pacific/Honolulu` を付けること。残り15本のスキップは v761〜v781 のバックアップが無いだけ。

**GitHub との突き合わせ（引き継ぎの記述を信じない・教訓⑳）**

```bash
curl -sL -o /tmp/gh.html https://raw.githubusercontent.com/motoi107/funergy-growth-os/main/index.html
md5sum /tmp/gh.html index.html
```

**v836〜v840 とも未 push。**

---

## 1. v840 でやったこと

### 入口を1本にした

| | 変更前 | 変更後 |
|---|---|---|
| ナビ | ダッシュボード ／ 管理ダッシュボード の2本 | **ダッシュボード 1本** |
| `dashboard` の対象 | ceo / gm / office / am | ＋ **chef / sl / g4** |
| `dashboard` の中身 | 従来のダッシュボード | **`mdCanOpen()` なら コンソール**、でなければ従来のまま |
| `mgmt_dash` | 独立ページ | **`dashboard` への別名**（保存済みリンクが切れないよう残す） |

**`renderDashboard()` / `renderSlFunergyNow()` / `canSeeDashboard()` は1文字も触っていない。**
経理（`mcRole()` が `''`）と G3・Crew の画面は今までどおり。

### コンソールの中身（PC 1180px 以上で2カラム）

**左（メイン）**

- **当日の状況** … Funergy Now（Toastの当日売上・既存の `loadRealtimeSales` をそのまま使用）
  ＋ 本日時点の進捗（**進捗予算**と比較）＋ 月末の着地予測 ＋ データ未入力の日数
  → **現在進捗と着地予測は別枠**（仕様§4）
- **月次KPI** … 1店舗なら実績／目標／差、複数店舗なら「**N / M 店舗達成**」＋未達店舗を名指し
- **店舗別の状況** … 判定／売上／人件費率／原価率／営業利益の確認表。
  **ランキングなし・順位番号なし。**未達を先に出すだけ。「全店 / 要確認」で切替
- KPI詳細（v842 のプレースホルダ）

**右（サイド）**

- **要対応** … 管理統括と**同じ** `management_alerts` の件数（別データを作らない）
- 社内メッセージ／仕入価格アラート（既存カードをそのまま取り込み）
- 店長への依頼（v845 のプレースホルダ）

### 守った決めごと

- **率の全社平均を新しく定義しない。** `mdScopeTotals` が合計するのは金額と日数だけ。
  率は店舗ごとに `mdVerdict` で判定し、**店舗数で数える**
- 達成／未達は**文字＋アイコン＋色の3つ**。色だけで判断させない
- 判定不可は 0 でも未達でもない。数値は `—`
- CSS は `.md-*` の中だけ。**640px以下の既存ルールには触っていない**
  （追加したモバイル向けは `@media (max-width:760px)` の1本のみ）

---

## 2. 進み具合

| 版 | 内容 | 状態 |
|---|---|---|
| v837 | 判定層（達成／未達／判定不可） | 完了 |
| v838 | 必須KPI設定（GM） | 完了 |
| v839 | コンソールの器（PC 2カラム・ヘッダー・全体判定） | 完了 |
| v840 | **ダッシュボード統合・当日／月次KPI／店舗別** | **完了** |
| v841 | KPI詳細パネル（推移・想定原因・既存分析画面への遷移） | 次 |
| v842 | 役職別の作り込み（Head Chef の原価・仕入中心 ／ CEO の絞り込み） | |
| v843 | Work Center の本統合（要対応／対応中／定期管理／承認待ち） | |
| v844 | 店長通知の画面連結 ＋ 管理統括の「整備中」を外す | |
| v845 | AI Help（Phase 2 の入口） | |

**次に見てもらうときは `node make_preview.js` → `preview.html`。**
GM（全社8店）と Store Manager（自店舗）の2視点が1枚に出る。
数字はサンプル。ブラウザ幅を 1180px より狭めるとスマホ表示に切り替わる。

---

## 3. v839 の出どころについて（記録）

v839 は**この作業の担当が書いたものではない**。v838 を包んだ直後（08:55）に
`index.html` を読んだところ、すでに `APP_VERSION=839` になっていた（09:03 更新）。
`index_v839_backup.html` / `verify_v839.js` / `preview.html` / `HANDOFF_v839.md` も
09:03〜09:09 で揃っていた。同じコンテナで並行して動いた作業と思われる。

そのまま渡さず、独立に検証した結果は以下。**問題は見つからなかった**ので v840 の土台に採用した。

- `fetch` / `eval` / ストレージ書き込み / 外部URL：**すべて0**
- 権限は `mdCanOpen()`＝`mcRole()!==''`、範囲は `mcScopeStores()`。**独自の権限判定なし**
- v838→v839 の差分は挿入200行＋置換2行（ナビ1行・版数1行）のみ。既存CSSは無傷
- `preview.html` は `<script>` 0個の静的HTML

**心当たりが無ければ、どこから来たものかは確認しておいたほうがよい。**

---

## 4. 未確認・保留

### ① 管理統括の残り

- **`management_control_v815.sql` の流し直しが未実施**（v824 の chef 追加ぶん）。
  `create or replace` なので何度流しても壊れない。SQL のみ
- 管理統括はまだ「整備中」表示中。v844 で外す
- **7月（前月）の検知結果を Moto さんがまだ見ていない**
- 理論消費と実消費の差は未実装（`fv_variance` が Totoya 専用のため）

### ② 昨対（2025年データ）

判断は「枠だけ作り判定不可表示」。入れるなら 同期センター → 期間指定取り込み（`doRangeImport`）で
2025年を引けるか実機確認から。入ったら `MD_KPI.yoy_sales` の `pending` を外し、
`mdVerdict` の yoy_sales 分岐を実装する。

### ③ `_LS_PRUNABLE` が死んでいる（v836 で発見・未着手）

`lsIsPrunable` は関数内の `_REGENERABLE` で判定しており `_LS_PRUNABLE` はどこからも使われていない。
掃除するときは第5章㉓の地雷リストを見ること（`verify_v827` / `verify_v828` が落ちる）。

### ④ v807 から持ち越し

- Tip入力権限の調査（v792 の診断画面の結果待ち）
- AM の管轄割り当ての実データ確認（v800 の画面）
- LaLa：Chika・Aya のロールが `chef` / `office_crew` になっているか
- `supabase/functions/` が未push、`hyper-worker` が未デプロイ

---

## 5. 教訓

v807 ①〜⑤、v825 ⑥〜⑫、v835 ⑬〜⑱、v836 ⑲〜㉑、v837 ㉒〜㉔、v838 ㉕㉖ は有効。追加分：

### ㉗ 検証に日付を固定しない

`verify_v814` の [5] が落ちた。**製品ではなく時計が回っただけ**だった。

```js
ok(f.sales.days === 8, '何日前かを出す');   ← 2026-08-02 前提の固定値
```

ハワイが 08-03 に入った瞬間に 9 になり、以後**永久に落ち続ける**作りだった。
実行日から計算するように直した。

```js
const _hiToday = new Date().toLocaleDateString('en-CA', { timeZone: 'Pacific/Honolulu' });
const _expDays = Math.round((new Date(_hiToday+'T00:00:00') - new Date('2026-07-25T00:00:00'))/86400000);
```

**入力データの日付を固定するのは良い。期待値に「今日からの日数」を書くのが危ない。**
他の検証にも `2026-08-0x` を含むものが14本ある（多くは入力データなので問題ない）。
同じ形で落ちたら、まず期待値が日付依存でないかを見ること。

### ㉖ の再発（自戒）

v838 で「置換文字列に改行が入るときは長さに関係なく `\\r\\n` を使う」と書いたのに、
v840 でまた素の `\\n` を3つ書いて `lone LF = 3` を出した。
手順どおり検出して前版に戻してからやり直した。

**大きいブロックは `crlf()` を通すので安全。危ないのは2〜4行の差し込み。**
`rep()` に渡す文字列に `\\n` が無いか、書いた直後に必ず見ること。

---

## 6. 重要な設計上の決定（勝手に変えないこと）

- **数字の計算を新しく作らない**（`cumulativeToToday` / `foodCostOf` / `laborCoreForYm` /
  `plComputeStore` / `ecCollect`）。判定層も表示層もこれらを**呼ぶだけ**
- **率の平均を画面ごとに定義しない。**複数店舗は「達成した店舗数」で数える
- **同じ判定・同じ式を2か所に書かない**
- **判定不可は 0 ではない。**数値は `null`、理由を必ず持たせる
- **未達ゼロでも判定不可が残っていれば「達成」と言い切らない**
- **ランキングを作らない。**順位番号も付けない
- 権限はアプリ層で判定する＋ RLS でも制限。`mcScopeStores()` を通す
- 店長は role では決まらない。`mcIsStoreManagerEmp()` が唯一の判定口
- 新キーは「合流ルール＋LS_NEVER_FREE＋SAMPLE」を同時に。
  **できれば既存キーに相乗りする。ただし相乗り先の既存の書き手を必ず読む**（教訓㉕）
- **640px以下の既存CSSに触らない。**コンソールの CSS は `.md-*` の中だけ

---

## 7. 同梱ファイル（追加・変更ぶん）

| ファイル | 用途 |
|---|---|
| `index.html` / `sw.js` | **v840 本体（GitHub 未 push）** |
| `preview.html` / `make_preview.js` | **画面プレビュー。**`node make_preview.js` で作り直せる |
| `verify_v840.js` | **新設**。統合コンソールの検証（PASS=79） |
| `index_v840_backup.html` | **新設**。次版の検証が「修正前」として使う |
| `verify_v814.js` | 日付固定を実行日基準に修正（教訓㉗） |
| `DESIGN_management_dashboard.md` | 仕様16章 → 既存コードの対応表 |
| `HANDOFF_v839.md` 〜 `HANDOFF_v807.md` | 過去の引き継ぎ |

---

## 8. 次のセッションの入り方

1. `TZ=Pacific/Honolulu node run_verify.js` → 第0章の期待値と一致するか
2. GitHub main を md5 で突き合わせる（**引き継ぎの記述を信じない**）
3. `node make_preview.js` で今の画面を見る
4. v841（KPI詳細パネル）から進める

**Motoさんの進め方**：「憶測で修正しない」。原因を確かめ、設計 → 確認 → 実装。
**Motoさんは実画面を見て的確に指摘する。**作り込む前にプレビューを見てもらうこと。

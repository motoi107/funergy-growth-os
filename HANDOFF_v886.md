# HANDOFF v886 — Funergy Growth OS

## 0. ⚠️ 毎回のリマインド（Motoさん依頼）

公開URLで配布しているため Supabase の anon キーがクライアントに露出しています。
`tip_labor` の RLS ポリシー `tip_labor_anon_all` が anon に ALL（qual=true / with_check=true）を
与えており、**URLとソースを見た人なら誰でもチップ・勤怠データを読み書きできる状態**です。

恒久対策は「読みは anon ／書きは認証済みのみ」または Edge Function 経由。
着手時は同期経路への影響範囲の洗い出しから。

**v873 以降、伝票画像のアップロードも同じ anon キーで Storage に直接 POST しています。**
対策を入れるときはこの経路が必ず影響を受けるので、含めて設計すること。

---

## 1. 現在地

```
APP_VERSION = 886 / SW_BUILD = 886
index.html  md5 38c62ef45c3035054d861bfef66ef72a
sw.js       md5 69e0a7921083cf45d2fcb1caf463cd40
CRLF=59906 / lone LF=0

verify_v886.js   PASS=23 FAIL=0    （v885 に当てると PASS=9 FAIL=14 ＝ 検知できている）
smoke_v886.js    renderAcctCenter() を 21 タブ全部で実行 → 全部 HTML を返す
new Function()   構文チェック OK（4,134,767 文字）
```

---

## 2. v885 の障害の原因（確定）

**`AC_TABS` / `_acCat` / `_acSub` の宣言が index.html から丸ごと消えていた。**

- `AC_TABS` … 参照 3 か所（32594 / 32600 / 32613）、代入 **0 か所**
- `_acCat` `_acSub` … 代入は `acSetCat()` と boot 経路にあるが、`var` 宣言が **0 か所**

`renderAcctCenter()` の 1 行目が `AC_TABS.find(...)` なので **必ず ReferenceError** で落ちる。
落ちると `el.innerHTML = renderAcctCenter()` の**代入自体が起きない**ため、
直前に描かれていた Office管理の DOM がそのまま残る。
Motoさんの報告 3 つ（GM の管理画面が出る／ボタンが効かない／挙動がおかしい）は
**これ 1 つで全部説明がつく**。HANDOFF_v885 の仮説 ③ が当たり。

仮説 ① は外れ（`OFFICE_OK_PAGES` に `acct_center` は入っていた）。
仮説 ② は当たっていたが主因ではない（別途 v886 で対応済み）。

### なぜ verify が緑だったか

検証は「文字列があるか」を見ていた。`AC_TABS` は**参照が 3 か所ある**ので
grep 系のチェックは全部通る。**「参照はあるが宣言が無い」を見ていなかった。**

---

## 3. v886 で入れた変更（4 点）

| # | 変更 | 目的 |
|---|---|---|
| 1 | `AC_TABS` / `_acCat` / `_acSub` / `acDashBody` / `acErrBody` を復元 | 描画できるようにする |
| 2 | `SKEL_LIGHT_PAGES` に `acct_center` を追加 | 起動直後にボタンが効かないのを解消 |
| 3 | `case 'acct_center'` を try/catch で包む | 落ちたとき前の画面を残さず、原因を画面に出す |
| 4 | APP_VERSION / SW_BUILD を 886 へ | 配布 |

### AC_TABS の約束ごと（崩すと静かに壊れる）

- `sub.id` は `_acPend()` が返す `'invoice','lost','gas','exp','vac'` と一致必須
  （ずれると未処理バッジの飛び先が存在しない中タブになる）
- `sub.list` は `_acRows()` が受ける `'invoice','receipt','reimb','lost','gas'` のみ
- `sub.fn` / `sub.more` は `window[名前]()` で**引数なし**に呼ぶ。
  引数が要る関数は `acDashBody` / `acErrBody` のように包んでから載せる

この 3 つは `verify_v886.js` が機械的に見張る。

---

## 4. ⚠️ 復元にあたって推定した部分（Motoさん確認おねがいします）

**確実**（コード側の契約から一意に決まる）:
経理レビューの 5 つのアップロード中タブ（Invoice / レシート / 建て替え / 紛失届 / ガソリン代）、
未処理バッジの飛び先、既定タブ（dash/dash）。

**HANDOFF_v885 の記述から復元**（＝元の定義そのものではない）:

- 経理レビューの残り 8 中タブの**並び順と `fn` の割り当て**
  （休暇申請 / 事前経費 / 仕入明細 / 業者マスター / 区分・承認 / エラーセンター / タスク / 報告・承認）
- **Tip＆シフトリーダー** … `renderTipMgmt` / `renderTipShiftLeader` の 2 中タブに分割
- **出力センター** … `renderAcctExport` 1 枚（棚卸・Tip が両方この関数の中にある）
- **人材** … `renderHR` / `renderGrowthHub` / `renderHiring` の 3 中タブ
- 各タブのアイコン

**ラベル・並び・分け方が違っていたら直します。** 中身が違っても壊れはしません
（`window[fn]` が無ければ「表示できる内容がありません」に落ちるだけ）。

---

## 5. 【最重要の教訓】新しいページを足すときに登録が必要な場所

**v879〜v885 の失敗はすべてこれ。**1か所でも漏れると、画面は「無い」のと同じか、
「ナビだけ変わって中身が古い」状態になる。

| 登録先 | 役割 | 漏れたときの症状 |
|---|---|---|
| `renderPage()` の `switch` | 描画 | 何も出ない |
| `NAV_ITEMS` | ラベル・アイコン・roles | サブナビに出ない |
| `MENU_GROUPS[].children` | 店舗表示のナビ | 店舗表示で出ない |
| `OFFICE_GROUPS[].children` | オフィス表示のナビ | **オフィス表示で出ない** |
| `OFFICE_OK_PAGES` | オフィス表示の許可リスト | **オフィスで何を押しても Office管理が出る** |
| `SKEL_LIGHT_PAGES` | コールドスタート中の描画 | 起動直後だけ反応しない |
| `_GROUP_COLOR` | グループの色（任意） | 色が既定になるだけ |

**v886 追加：構成テーブル（AC_TABS のような「唯一の定義」）を持つ画面は、
そのテーブルの宣言が生きているかも見ること。** 参照だけ残って宣言が消えても
grep 系の検証は全部通る。`verify_v886.js` の「宣言がある: X」がこれ。

`roles` は**メニューの表示可否だけ**で `renderPage()` は塞がない。

---

## 6. 未確認・未着手（v885 から持ち越し）

1. **v873 の画像退避** — 機内モードで登録 →「端末に保存しました」→ 解除 →「送信しました」の実機確認
2. **7/10〜7/31 のレシート** — 復旧不能。紙が残っていれば撮り直し、無ければ紛失届で処理
3. **経理レビューの一覧から直接ステータス変更**（確認済みにする／差し戻す）は未実装
4. **エラーセンター・タスク・報告承認の件数**は `_acPend()` に入っていない
5. **備品発注・発注**は「準備中」として経理から外したまま
6. **通知**は指示どおり経理から削除済み

### v886 で新たに提案（未着手）

7. **`switch` 全体を try/catch で包む。** 今回 `acct_center` だけ包んだが、
   「描画関数が落ちる → 前の画面が残る → アプリが壊れたように見える」は
   どの `case` でも起こる。同じ症状で 3 版つぶしたので、全体に広げる価値がある。
   `case 'mgmt_dash'` のような早期 `return` の意味は変わらないので安全。
8. **`verify` に「参照はあるが宣言が無い識別子」の全体スキャンを追加。**
   今回は経理センターだけ手で見つけた。ファイル全体でやれば同じ事故を防げる。

---

## 7. 進め方について

**変更のたびに「どこを開いて何が見えれば成功か」を先に書き、Motoさんに確認してもらうこと。**

v886 の成功条件（この 4 つが揃えば直っている）:

1. 本部表示 → 経理センターを開く
2. 上に**大タブが 6 つ**（経理ダッシュボード / 経理レビュー / Tip＆シフトリーダー / 出力センター / 勤怠 / 人材）
3. 最初に出るのは**経理ダッシュボード**（＝旧 Office管理の中身、帯なし）
4. 経理レビューを押すと**中タブが 13 個**並び、先頭に未処理サマリーが出る

どれか 1 つでも違ったら、その時点で止めて症状を教えてください。
**赤い警告カードが出た場合は、そこに出ている文言をそのまま送ってください**（v886 で
落ちた事実が画面に出るようにしたため、原因が特定できます）。

また、`verify_v803` は invoices の保存箇所が増えていないかを見張っている。
`pimgFlush` と `saveInvoiceMemo` を名指しで除外済み。
**新しい保存経路を足したらここに当たる。数字を上げるだけにせず、条件つきで除外すること。**

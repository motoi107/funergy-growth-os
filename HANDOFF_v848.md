# Funergy Growth OS — 引き継ぎ v848

**v848：建て替えレシートの確認／ガソリン代の月次出力／IRS標準料率のズレ。**

---

## 0. ⚠️ 毎回のリマインド（Motoさん依頼）

**公開URLで配布しているため Supabase の anon キーがクライアントに露出しています。**
`tip_labor` の RLS ポリシー `tip_labor_anon_all` は anon に **ALL（qual=true / with_check=true）** を与えており、
**URLとソースを見た人なら誰でもチップ・勤怠データを読み書きできる**状態です。

恒久対策は「読みは anon／書きは認証済みのみ」または Edge Function 経由。
着手するときは同期経路への影響範囲の洗い出しから。今回も未着手です。

---

## 0.5 現在地（ひと目で）

| | 状態 |
|---|---|
| 現行版 | **v848**（`index.html` / `sw.js`）。**v836〜v848 は GitHub 未 push** |
| 検証 | `PASS=4137 / FAIL=0 / 実行=72/87`。検証87本・バックアップ67件（v782〜v848） |
| 直近の大きな仕事 | Management Console（v837〜v843）／経理まわり（v844・v846・v848）／シフトテスト用スタッフ（v847） |
| **Motoさん待ち①** | **ガソリン代の単価を $0.76 に直す**（IRS 2026/7/1〜。現在 $0.70 のまま＝支給不足の可能性） |
| **Motoさん待ち②** | **KPI管理で 人件費率・原価率の目標**＋**本部予算**を入れる（入るまで全8店が判定不可） |
| **Motoさん待ち③** | `management_control_v815.sql` の流し直し（v824 の chef 追加ぶん） |
| 確認待ち | チップ調整の保存（SQLは適用済み）／事務Crewの権限／レシート一覧／月次CSV |
| 積み残し（大） | **公開URL／anonキー**（第0章）・管理統括の「整備中」・昨対の2025年データ |

**Motoさんは実画面を見て的確に指摘する。**この数版の不具合は全部そこから見つかった。
作り込む前にプレビューか実機を見てもらうこと。

---

## 1. 最初にやること

```bash
unzip funergy_v848_handoff.zip
tar -xJf handoff_v848.tar.xz
cd handoff_v848
TZ=Pacific/Honolulu node run_verify.js
```

**期待値**

```
合計 PASS=4137  FAIL=0  実行=72/87
合流ルールの未定義: 1
同期対象で未保護: 0
予算の対象月: 3 / 7
APP_VERSION=848 / SW_BUILD=848
CRLF=57887 / lone LF=0
```

**v836〜v848 とも未 push。**

---

## 2. ⚠️ IRS標準料率が2回変わっています（要対応）

アプリの単価の既定は **$0.70**。これは **2025年の値**です。

| 期間 | IRS標準料率（business） |
|---|---|
| 2025年 | $0.70 |
| **2026/1/1〜6/30** | **$0.725** |
| **2026/7/1〜** | **$0.76** |

2026年は年央で改定されました（ガソリン価格上昇による、2022年以来初の年央調整）。
出典：IRS Notice 2026-10 / Announcement 2026-29。

**設定の単価が $0.70 のままだと、支給額が不足している可能性があります。**
ガソリン代の画面に、設定値と現在の料率を並べた注意を出すようにしました。
**変更は「設定」から。アプリが勝手に書き換えることはしていません。**
過去の申請は当時の単価のまま残ります（あとから遡って書き換えない）。

---

## 3. v848 でやったこと

### A. 建て替え経費のレシート（Invoice と同じ扱いに）

**レシート画像は元から `imagePath` に保存されていて、Invoice と同じ `invoices` バケットに入っていました。**
`_rbRow` にも画像ボタンがありました。**見られなかった理由は表示の絞り込みです。**

```js
var paid = scoped.filter(承認 && 支払済).slice(0, 10);   ← 直近10件だけ
var rej  = scoped.filter(却下).slice(-5);                 ← 直近5件だけ
```

**承認・支払が済んだ11件目以降は画面から消えていた**ので、経理が過去のレシートを確認する手段がありませんでした。

**直したこと**

- **件数の上限を撤廃**（20件でも全部出ることを検証で固定）
- **月で絞れる**ようにした（新しい順＋「すべて」）
- **状態で絞れる**ようにした（すべて／承認待ち／未払い／支払済／却下・各件数つき）
- **件数・合計金額・レシート件数**を出す
- レシートのボタンに **「レシート」** と文字を入れた（アイコンだけでは気づけない）
- レシートが無い申請は **「レシートなし」** と出す
- `office_crew`（Akane）も見られるようにした（承認ボタンは出ない。v846 のまま）

### B. ガソリン代の月次出力

**CSV：日付／スタッフ／店舗／行先／内容／距離(マイル)／単価／金額／往復／承認者／承認日時**

- 行先は「出発 → 到着」、内容は用途＋メモ
- **金額は申請時に確定した単価で計算済みの値をそのまま使う**（あとから料率を変えても過去は動かない）
- 末尾に **合計** と **スタッフ別の精算額**（そのまま精算に使える）
- 末尾に **その月のIRS標準料率**を記載
- ファイル名 `ガソリン代_2026-07.csv`、Excel向けにBOM付き
- **承認済みのみ**。月の選択は承認済みがある月だけ出る
- 出せるのは `canViewMileageAll()`＝経理（office / office_crew）と従来の管理者。
  **月末処理の権限が無くても出せる**（精算は経理の仕事なので）

---

## 4. 未確認・保留

### ① クレジットTip調整（v845〜）

`tip_credit_adj_v848.sql` は **適用済み（8列 OK）**。RLS も問題なし。
**実機で保存できたかの確認待ち。** 失敗したら画面に理由が出るので、その文言を教えてください。

### ② v846・v847 の実機確認

- Akane（事務Crew）：承認を押すと「権限がありません。事務Crewは閲覧のみです」が出るか
- Akane：ガソリン代の精算が見えるか
- シフトテスト用スタッフ15名（設定画面から作成／削除）

### ③ Management Console（v837〜v843）

**KPI目標と本部予算が未設定のため、人件費率・原価率・営業利益・客数・客単価が全8店で判定不可。**
KPI管理 → 店舗タブで目標を入れると判定が出る。

### ④ その他

- **`management_control_v815.sql` の流し直しが未実施**（v824 の chef 追加ぶん）
- 管理統括はまだ「整備中」表示中
- 経理に `原価管理`・`食材マスタ` を残してよいか／`設定` を事務Crew に見せてよいか（各1行）
- 昨対（2025年データ）… 枠だけ
- `_LS_PRUNABLE` が死んでいる（v836 で発見・未着手）
- `supabase/functions/` が未push、`hyper-worker` が未デプロイ
- **v839 の出どころ**（並行作業のもの。`HANDOFF_v840.md` 第3章）

---

## 5. 教訓

v807 ①〜⑤、v825 ⑥〜⑫、v835 ⑬〜⑱、v836 ⑲〜㉑、v837 ㉒〜㉔、v838 ㉕㉖、v840 ㉗、
v841 ㉘、v842 ㉙㉚、v843 ㉛㉜、v844 ㉝㉞㉟、v845 ㊱㊲㊳、v846 ㊴㊵㊶、v847 ㊷㊸㊹㊺㊻㊼ は有効。追加分：

### ㊽ 「見えない」の原因は、機能が無いことより表示の上限であることが多い

「承認後のレシートが確認できない」と聞いて、まず保存されていないことを疑ったが、
**画像も `imagePath` もボタンも最初からあった。**原因は `slice(0,10)` の1行。

**「無い」と言われたら、まず表示側の filter / slice / 折りたたみを読むこと。**
作る前に、既にあるものが出ていないだけではないかを確かめる。

### ㊾ 一覧に上限を入れるときは、上限に当たったことを画面に出す

`slice(0,10)` は「支払済ログ（直近10件）」と書いてはいたが、
**11件目以降にたどり着く方法が無かった。**上限があること自体は悪くないが、
その先へ行く手段（月で絞る・全件表示）を必ず用意する。

### ㊿ 外部の基準値は、日付つきの表で持ち、勝手に適用しない

IRS標準料率は2026年に2回変わった（1/1 $0.725、7/1 $0.76）。
アプリの既定は $0.70（2025年の値）のままだった。

- **表で持つ**：`IRS_MILEAGE_RATES` に発効日つきで並べる。変わったら1行足す
- **過去は動かさない**：実績は申請時に保存した `r.rate` を使う
- **勝手に書き換えない**：設定値とのズレを知らせるだけ。変更は人が決める

**外部の基準（税率・料率・最低賃金）をコードに埋めるときは、発効日つきの表にする。**
定数1つにすると、変わったときに過去まで書き換わる。

---

## 6. 重要な設計上の決定（勝手に変えないこと）

- **公開URL／anonキーの件は毎回リマインドする**（第0章）
- 事務Crew（office_crew）は閲覧のみ。決める操作は `denyViewOnly()` で止める
- 「見える」と「できる」を別のゲートにする
- 経理は office / office_crew で同じメニューを見せる
- **DBの列を使うコードを書いたら、同じ版で SQL を同梱し、実際に流して確かめる**
- **失敗の文言は「次の一手」まで書く**
- **一覧に上限を入れたら、その先へ行く手段を用意する**（教訓㊾）
- **外部の基準値は発効日つきの表で持ち、過去は動かさない**（教訓㊿）
- 数字の計算を新しく作らない。判定不可は 0 ではない。ランキングを作らない
- 640px以下の既存CSSに触らない

---

## 7. 適用が必要な SQL

| ファイル | 状態 |
|---|---|
| `management_control_v808/811.sql` | 適用済み |
| `management_control_v815.sql` | **v824 で chef 追加。流し直しが必要** |
| `management_control_v822_cleanup.sql` | 適用済み |
| `tip_credit_adj_v848.sql` | **適用済み（8列 OK・確認済み）** |

**確認用（読み取りのみ）**：`check_tip_adj_v848.sql` / `check_mc_v818.sql` / `check_toast_sync.sql`

---

## 8. 同梱ファイル（追加・変更ぶん）

| ファイル | 用途 |
|---|---|
| `index.html` / `sw.js` | **v848 本体（GitHub 未 push）** |
| `verify_v848.js` | **新設**。レシートの全件表示・月次CSV・IRS料率の検証（PASS=85） |
| `index_v848_backup.html` | **新設** |
| `tip_credit_adj_v848.sql` / `check_tip_adj_v848.sql` | 適用済み。再実行しても壊れない |
| `HANDOFF_v847.md` 〜 `HANDOFF_v807.md` | 過去の引き継ぎ |

---

## 9. 作業のしかた（この環境でのメモ）

### ビルド

1. `cp index.html index_v<今の版>_backup.html`（変更**前**に）
2. Python の透過置換。**`rep()` は old / new とも CRLF に寄せてから使う**
   （v842・v843 で片側だけ寄せて2回つまずいた）
3. 新規の大きいブロックは LF で書いて `crlf()` を通す
4. `APP_VERSION` と `SW_BUILD` を同時に上げる
5. Node の `new Function()` で構文チェック
6. **書き出し前に `assert lone LF == 0`**
7. 前版と差分を取り、意図した範囲だけかを目で見る
8. `TZ=Pacific/Honolulu node run_verify.js`
9. 最後に `cp index.html index_v<新版>_backup.html`
10. `/mnt/user-data/outputs/` へコピー → `present_files`

### SQL は必ず実行してから渡す（v845 の反省）

このコンテナに PostgreSQL を入れられる。**`apt-get update` を先に打つこと**
（打たずに `install` すると 404 で落ちる）。

```bash
apt-get update -qq && apt-get install -y --no-install-recommends postgresql
export PGBIN=/usr/lib/postgresql/16/bin
mkdir -p /tmp/pgdata && chown -R postgres /tmp/pgdata
su postgres -c "$PGBIN/initdb -D /tmp/pgdata -U postgres"
su postgres -c "$PGBIN/pg_ctl -D /tmp/pgdata -l /tmp/pg.log -o '-k /tmp -p 5433' start"
su postgres -c "$PGBIN/psql -h /tmp -p 5433 -U postgres -f your.sql"
```

**Supabase の SQL エディタは全体を1トランザクションで流す。**
途中で落ちると成功していた `alter table` もロールバックされるので、
`--single-transaction` を付けて同じ条件で試すこと。
**診断用SQLは「壊れている状態」でも流して確かめる**（正常系だけでは意味がない）。

### 画面を見てもらう

```bash
node make_preview.js      # → preview.html（Management Console のプレビュー）
```

### 気をつけること

- 検証が落ちたら、**まず検証を疑う**（この数版はほぼ全部そうだった）
- **定義を消す前に `grep -l '<名前>' verify_*.js` を打つ**（消すと PASS=0 で止まる）
- 検証に**日付や時刻の「変化」を書かない**（日が変わると／同一ミリ秒だと落ちる）
- **同じコンテナで並行作業が走っていたことがある**（v839。`HANDOFF_v840.md` 第3章）。
  作業前に `index.html` の `APP_VERSION` と md5 を確認する

---

## 10. 次のセッションの入り方

1. **第0章（公開URL／anonキー）のリマインドを伝える**
2. `TZ=Pacific/Honolulu node run_verify.js` → 第1章の期待値と一致するか
3. GitHub main を md5 で突き合わせる（**引き継ぎの記述を信じない**）
4. 第0.5章の「Motoさん待ち」を確認する
5. 続きへ

**Motoさんの進め方**：「憶測で修正しない」。原因を確かめ、設計 → 確認 → 実装。
分からないことは推測で埋めず聞く。

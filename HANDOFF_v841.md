# Funergy Growth OS — 引き継ぎ v841

**v841：コンソールをフル幅・高密度にし、承認・タスク・エラーも1画面に入れた。**

---

## 0. 最初にやること

```bash
unzip funergy_v841_handoff.zip
tar -xJf handoff_v841.tar.xz
cd handoff_v841
TZ=Pacific/Honolulu node run_verify.js
node make_preview.js        # → preview.html。ブラウザで開く
```

**期待値**

```
合計 PASS=3564  FAIL=0  実行=65/80
合流ルールの未定義: 1        ← spl_skills_st_ の1件だけが既知
同期対象で未保護: 0
予算の対象月: 3 / 7
APP_VERSION=841 / SW_BUILD=841
CRLF=57263 / lone LF=0
```

`TZ=Pacific/Honolulu` を付けること。残り15本のスキップは v761〜v781 のバックアップが無いだけ。

**GitHub との突き合わせ（引き継ぎの記述を信じない・教訓⑳）**

```bash
curl -sL -o /tmp/gh.html https://raw.githubusercontent.com/motoi107/funergy-growth-os/main/index.html
md5sum /tmp/gh.html index.html
```

**v836〜v841 とも未 push。**

---

## 1. v841 でやったこと

### フル幅にした（この画面のときだけ）

アプリ全体は `.main { max-width: 900px }` で、`body{zoom:1.1}` が掛かるため実効 約818px。
これがコンソールの密度を制限していた。

**`renderPage` で `body.md-wide` を付け外しし、この画面のときだけ上書きする。**

```
1180px 以上 → .main を 1500px まで
1560px 以上 → .main を 1880px まで
```

**既存の `.main` の指定そのものは変えていない。**他の画面は今までどおり 900px。
`dashboard` / `mgmt_dash` 以外を描くと自動で外れる。

### 3カラムにした

```
1560px 以上： 左＝当日の状況・重要KPI ／ 中＝店舗別 ／ 右＝承認・タスク・エラー
1180px 以上： 左＝当日・KPI・店舗別   ／ 右＝承認・タスク・エラー
それ以下  ： 1カラム（スマホ）
```

グリッドは `grid-template-areas` で組んでいるので、DOM の並びを変えずに段組だけ変わる。

### 上帯（全幅）に主要な数字を7つ並べた

| 枠 | 中身 |
|---|---|
| 全体判定 | 達成／未達／判定不可 ＋「N / M 店舗達成」 |
| 今日の売上 | Toast の当日売上（既存の `loadRealtimeSales` をそのまま使用） |
| 本日時点の進捗 | 進捗予算比％ ＋ 実績／進捗予算 ＋ 差 ＋ **帯** |
| 月末の着地予測 | 予測額 ＋ 月間予算 ＋ 予測差 ＋ **帯** |
| 要対応 | 管理統括の未対応・対応中 |
| 承認・タスク・エラー | `承認 / タスク / エラー` を1枠に。エラーは要対応の内訳つき |
| データの入り | 売上が未入力の日数（判定不可の理由になる） |

**スクロールせずに 今日・今月・重要な件数が全部見える**ことを狙っている。

### 承認・タスク・エラーを右レールに統合

- **承認待ち** … `ls('approvals')` の pending。**表示範囲で絞る**（AM は管轄外を見ない）
- **本日のタスク** … `getTasks()` の未完了で、期限が今日以前のもの（先の予定は出さない）
- **エラーセンター** … `ecCollect()`。重要度は既存の `ecSevMeta()` の色分けをそのまま使用
- それぞれ「ひらく」で既存画面へ

**新しいデータは1つも作っていない。既存の入れ物を読むだけ。**
コンソールから書き込みは一切しない（検証で固定）。

### 数字の可視化

`mdBar(rate, color)` … 達成率の帯。**目標線（100%）を 71.4% の位置**に置き、140% で振り切る。
目標を超えたか一目で分かる。下限型（人件費率・原価率）は超過が右に伸びるので、同じ見方で読める。

**判定不可には帯を付けない。**（0% に見えてしまうため）

---

## 2. 進み具合

| 版 | 内容 | 状態 |
|---|---|---|
| v837 | 判定層 | 完了 |
| v838 | 必須KPI設定（GM） | 完了 |
| v839 | コンソールの器 | 完了 |
| v840 | ダッシュボード統合 | 完了 |
| v841 | **フル幅・高密度・承認/タスク/エラー統合** | **完了** |
| v842 | KPI詳細パネル（推移・想定原因・既存分析画面への遷移） | 次 |
| v843 | 役職別の作り込み（Head Chef の原価・仕入中心 ／ CEO の絞り込み） | |
| v844 | Work Center の本統合 ＋ 管理統括の「整備中」を外す | |
| v845 | 店長通知の画面連結 | |

---

## 3. 未確認・保留

### ① 管理統括

- **`management_control_v815.sql` の流し直しが未実施**（v824 の chef 追加ぶん）。SQL のみ
- 管理統括はまだ「整備中」表示中。v844 で外す
- **7月（前月）の検知結果を Moto さんがまだ見ていない**
- 理論消費と実消費の差は未実装（`fv_variance` が Totoya 専用のため）

### ② 昨対（2025年データ）

「枠だけ作り判定不可表示」で確定。入れるなら 同期センター → 期間指定取り込み（`doRangeImport`）で
2025年を引けるか実機確認から。入ったら `MD_KPI.yoy_sales` の `pending` を外す。

### ③ `_LS_PRUNABLE` が死んでいる（v836 で発見・未着手）

掃除するときは第4章㉓の地雷リストを見ること（`verify_v827` / `verify_v828` が落ちる）。

### ④ v807 から持ち越し

- Tip入力権限の調査（v792 の診断画面の結果待ち）
- AM の管轄割り当ての実データ確認（v800 の画面）
- LaLa：Chika・Aya のロールが `chef` / `office_crew` になっているか
- `supabase/functions/` が未push、`hyper-worker` が未デプロイ

### ⑤ v839 の出どころ（記録）

v839 はこの作業の担当が書いたものではなく、同じコンテナで並行して動いた作業のものだった。
独立に検証して問題が無かったため土台に採用している（詳細は `HANDOFF_v840.md` 第3章）。
**心当たりが無ければ確認しておいたほうがよい。**

---

## 4. 教訓

v807 ①〜⑤、v825 ⑥〜⑫、v835 ⑬〜⑱、v836 ⑲〜㉑、v837 ㉒〜㉔、v838 ㉕㉖、v840 ㉗ は有効。追加分：

### ㉘ アンカーは「一意になるまで」広げる

`var el = document.getElementById('main-content');` を差し込み位置にしたら **2件**当たった。
`assert count == 1` が止めてくれたので、前後の行を足して一意にした。

**1行だけを狙わず、その関数だけに出る形まで広げること。**
とくに `document.getElementById(...)` のような定型行は何度も出る。

### ㉖ の再発防止（3回目）

v838 で書き、v840 で再発させた「置換文字列の改行は `\r\n`」を、v841 では最初から守れた。
`crlf()` を通す大ブロックと、`rep()` に直書きする短い差し込みで扱いが違うことを忘れないこと。

### 既存の見た目を広げるときは「上書き」で入れる

`.main { max-width:900px }` を書き換えれば全画面が広がるが、他の画面のレイアウトが崩れる。
**`body.md-wide .main` で、その画面のときだけ上書きする。**
クラスの付け外しは `renderPage` の1か所に置き、描画のたびに `toggle` する
（画面を離れたときに外し忘れない）。

---

## 5. 重要な設計上の決定（勝手に変えないこと）

- **数字の計算を新しく作らない**（`cumulativeToToday` / `foodCostOf` / `laborCoreForYm` /
  `plComputeStore` / `ecCollect`）。コンソールは**呼ぶだけ**
- **率の平均を画面ごとに定義しない。**複数店舗は「達成した店舗数」で数える
- **判定不可は 0 ではない。**数値は `null`、帯も付けない、理由を出す
- **未達ゼロでも判定不可が残っていれば「達成」と言い切らない**
- **ランキングを作らない。**順位番号も付けない
- **コンソールは読むだけ。**承認・タスク・エラーの状態をここで書き換えない
- 承認・タスクは**表示範囲で絞る**（AM が管轄外を見ない）
- 権限はアプリ層で判定する＋ RLS でも制限。`mcScopeStores()` を通す
- **640px以下の既存CSSに触らない。**コンソールの CSS は `.md-*` と `body.md-wide` だけ
- 新キーは「合流ルール＋LS_NEVER_FREE＋SAMPLE」を同時に。できれば既存キーに相乗りし、
  **相乗り先の既存の書き手を必ず読む**（教訓㉕）

---

## 6. 同梱ファイル（追加・変更ぶん）

| ファイル | 用途 |
|---|---|
| `index.html` / `sw.js` | **v841 本体（GitHub 未 push）** |
| `preview.html` / `make_preview.js` | 画面プレビュー。`node make_preview.js` で作り直せる |
| `verify_v841.js` | **新設**。フル幅・上帯・承認/タスク/エラーの検証（PASS=78） |
| `index_v841_backup.html` | **新設**。次版の検証が「修正前」として使う |
| `DESIGN_management_dashboard.md` | 仕様16章 → 既存コードの対応表 |
| `HANDOFF_v840.md` 〜 `HANDOFF_v807.md` | 過去の引き継ぎ |

---

## 7. 次のセッションの入り方

1. `TZ=Pacific/Honolulu node run_verify.js` → 第0章の期待値と一致するか
2. GitHub main を md5 で突き合わせる（**引き継ぎの記述を信じない**）
3. `node make_preview.js` で今の画面を見る
4. v842（KPI詳細パネル）から進める

**Motoさんの進め方**：「憶測で修正しない」。原因を確かめ、設計 → 確認 → 実装。
**Motoさんは実画面を見て的確に指摘する。**作り込む前にプレビューを見てもらうこと。

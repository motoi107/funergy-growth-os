# Funergy Growth OS 引き継ぎ（v773 → v774）

## 現在の状態

- `index.html` APP_VERSION = **774** / `sw.js` SW_BUILD = **774**（一致）
- CRLF 50,518 ・lone LF = 0 ・構文チェック OK
- 検証：`v764` / `v767` / `v769` / `v770` / `v771` / `v772` / `v773` / **`v774`** すべてPASS（合計 280項目）
- 監査：`audit_merge_coverage.js` MISS は `spl_skills_st_` の1件のみ／
  `audit_budget_month.js` 要確認3・解決済み7（**v772から変化なし**）

---

## ⚠️ デプロイ状況

| 版 | 状態 |
|---|---|
| 〜v772 | 本番反映済み・実機確認済み |
| v773 | **未デプロイ**（容量警告・自動pruneの撤去） |
| v774 | **未デプロイ**（本部目標の設定タブ） |

**v773 と v774 をまとめて出すことになる。** v773 は保存層に関わるので、
実機で一周（設定＞保存容量が緑・警告なし／勤怠・チップ・予算が通常どおり）してから
v774 の設定タブへ進むこと。

---

## このセッションでやったこと（v774）

### 本部目標を設定タブで一元管理し、月間行動計画へ反映

**背景**：本部目標(`hq_goals`)は毎月フリーテキストで入力、目標値(`cc_scores.target`)は
店舗×指標ごとにモーダルを開いて手入力していた。8店舗×指標数ぶん、毎月やり直しになる。

**Motoさんの指定（確認済み）**

| 項目 | 内容 |
|---|---|
| 原価管理 | 店舗ごとに％を固定（修正可能） |
| 衛生管理 | 基本100% |
| メニュー開発 | グランド更新＝ToriTon / Tenkichi / **Marujuu**、シーズナル＝全店（任意） |
| 人材・育成 | G3・G2の人数目標を**店舗ごと**に |
| グランドの実施月 | **固定しない（任意）** |
| Waikiki 2店舗 | FSP(F03) と Waikiki Garlic Shack(F03-G) |

**実装した順序（v762→765 と同じ考え方）**

「先に読み出しの出口を1本にしてから、中身を差し替える」。
目標を読んでいる箇所は6つ（差の計算・達成判定・集計・店舗カード・数値モーダル・DOHモーダル）あり、
先に設定を入れていたら、それぞれが別の値を見たまま静かに食い違っていた。

1. `ccScoreView(ym, storeId, mid)` を新設し、6箇所の `sc.target` 直読みを置換。
   **設定が空なら `ccScore()` と完全に同じものを返す**ので、この時点では挙動不変
2. 指標(`cc_metrics`)に `targets`（店舗別既定目標）と `req`（必須/任意）を追加
3. 設定タブを新設（GM/CEOのみ編集、Chefは閲覧）
4. 承認時に `ccFreezeTargets()` で目標を月へ書き込んで固定

**目標の解決規則**

- 月の数値に目標が入っていれば **それが勝つ**（その月だけの上書き）
- 入っていなければ設定の既定目標を使う
- **承認した時点で確定値を書き込む。以後、設定を変えても承認済みの月は動かない**（予算と同じ考え方）

**指標の再編**

| id | グループ | 対象店舗 | 必須 |
|---|---|---|---|
| `m_cost` 原価率 | 原価管理 | 全店 | ○ |
| `m_theo` 理論原価との差異 | 原価管理 | 全店 | |
| `m_doh` DOH適合 | 衛生管理 | 全店（既定100%） | ○ |
| `m_grand` グランドメニュー更新 | メニュー開発 | F01 / F02 / F05 | 任意 |
| `m_seasonal` シーズナルメニュー | メニュー開発 | 全店 | 任意 |
| `m_g3` G3キッチンリーダー | 人材・育成 | 全店 | ○ |
| `m_g2` G2 | 人材・育成 | 全店 | ○ |

実施月を固定しない方針のため、**メニュー開発は既定を任意**にした。必須にすると毎月ゲートに
引っかかる。締めたくなったら設定タブのチェックボックスで切り替えられる。

**`_ccMetricsMap()` を「既定 ∪ 保存（保存が勝つ）」へ**

従来は保存が1件でもあれば既定を完全に無視していた。そのため既定に足した指標（`m_g2` 等）が、
**既に設定を保存している端末には永久に現れなかった。** 合流方式に変えたので自動で現れる。
論理削除(`_deleted`)した指標は保存側が勝つので消えたまま。

**提出時の注意喚起**（`ccReqGaps`）
必須の指標に目標があるのに、その店の行動が1件も無ければ提出時に一覧で知らせる。
**止めはしない**（任意のメニュー開発まで止めると毎月引っかかるため）。

### 検証ハーネスの修正

v769 / v770 / v771 が落ちた。切り分けた結果：

- **v770・v771・v769 … 抽出漏れ（ハーネス側）**：`ccGroupAgg` / `ccSubmit` / `ccGroupSummary` が
  `ccScoreView` `ccReqGaps` を呼ぶようになったのに、各ハーネスの関数リストに入っていなかった。
  v771 は定数（`CC_DEFAULT_METRICS` / `CC_CAT_GROUP` / `STORES`）も渡していなかった
- **v769 … 設計変更（製品側が正しい）**：「メニュー開発は1指標に統合されている」「全店対象」という
  v769 当時の判断を、Motoさんの指定（グランド＋シーズナルに分割、対象店舗が異なる）が上書きした。
  アサーションを新しい構成へ更新した

---

## Motoさんの手作業が必要なもの

| # | 内容 | 理由 |
|---|---|---|
| 1 | **設定タブで各店の原価目標％を入力** | 既定値を入れていない。GMが入力する（v774） |
| 2 | **G3・G2 の人数目標を店舗ごとに入力** | 同上（v774） |
| 3 | **旧指標 `m_dev`（新メニュー・シーズナル進捗）を削除** | 保存済みなら残る。`m_grand`/`m_seasonal` と重複するので画面から削除（v774） |
| 4 | **Corporate Chef の従業員マスターで role を `chef` に変更** | これをしないと本部ログインの候補に出ない（v766） |
| 5 | **DOHチェック項目27件を実際の検査票と突き合わせ** | 既定値。Motoさん・Chikaさんの確認が必要（v769） |
| 6 | **Marujuu 7月の予算を入れ直す** | v761から持ち越し |
| 7 | 入れ直す前に**6月に見覚えのない数字が無いか確認** | v761から持ち越し |
| 8 | **他店舗の当月予算が意図した数字か確認** | v761から持ち越し |
| 9 | **Kaimuki / Piikoi のスキルレベルを一度保存し直す** | v761から持ち越し。1回上書きすれば以後正常 |
| 10 | **Career Score も一度保存し直す** | 同上 |
| 11 | v752 のスキル修正について**スタッフからの確認結果**を待ち | v761から持ち越し |

---

## 次回の優先タスク

1. **v773・v774 をデプロイして実機で一周**
2. **hq_goals（フリーテキストの本部目標）の整理** — 設定タブが目標の本体になったので、
   `hq_goals` は「その他の申し送り」として残すか、廃止するかを決める。
   いまは**両方が「本部目標」として画面に出る**ので、運用してみて判断したい
3. **指標ベースのカバー率** — 現在のカバー率は `hq_goals` に対する行動の紐づけを見ている。
   設定タブの必須指標に対するカバー率へ寄せるかは、2 と一緒に決める
4. **二重書きの停止**（IDB移行の最終段階）
5. **既存データとの連携** — 原価率を予実から、人員をシフトから、育成をスキルから自動取得
   （現在は手入力。二重入力になっている）。設定タブができたので目標側は自動化済み、次は実績側
6. **商品開発フロー** — 提案→試作→試食→AM以上の最終承認→販売準備→販売後評価
7. 持ち越し：①全スタッフへの従業員ディスカウント証明カード（マイページ内新設）
   ②Tip入力権限の調査（v626でG1解放・v630でg2正規化済みにもかかわらず入力できないスタッフが存在）
8. HANDOFF_v747 から未着手：Qサマリー追加項目の判断、「翌月」問題の全画面点検、日次食材差異の初期設定

---

## 検証・監査スクリプト

| ファイル | 用途 |
|---|---|
| `verify_v762`〜`v768` | 前版の `index_v76x_backup.html` が要る（同梱していないと ENOENT） |
| `verify_v769` | グループ化・DOH・行動の振り分け（**v774でメニュー開発の検査を更新**） |
| `verify_v770` | 差の計算・前月比・店舗別行動（**v774で抽出リストを更新**） |
| `verify_v771` | 承認後の固定・結果報告・継続（**v774で抽出リストと定数を更新**） |
| `verify_v772` | 週次更新 |
| `verify_v773` | 容量警告・自動pruneの撤去 |
| **`verify_v774.js`** | **設定タブと目標の反映。11観点37項目。v773の症状を再現・比較** |
| `audit_merge_coverage.js` | 合流ルール網羅チェック。MISSが `spl_skills_st_` の1件だけであること |
| `audit_budget_month.js` | 予算まわりで対象月を解決していない関数の洗い出し |

**次回のzipには `index_v774_backup.html`（今回の index.html のコピー）を同梱すること。**

---

## このセッションで学んだこと

1. **読み出しの出口を1本にしてから、値の出どころを変える。** 目標を読んでいる箇所は6つあった。
   先に設定を入れていたら、達成判定・差の計算・集計・カードが別々の値を見たまま食い違っていた。
   v762→765 の保存層と同じ構図
2. **「保存があれば既定を無視」は、後から項目を足せなくなる。** `_ccMetricsMap()` がこれで、
   既定に足した指標が、設定を保存済みの端末には永久に現れなかった。合流（保存が勝つ）にすると
   追加は自動で届き、削除は削除のまま残る
3. **ゲートは、運用が回るところから締める。** 実施月を固定しないなら、グランドを必須にすると
   毎月引っかかる。既定は任意にして、締めるかどうかは画面で切り替えられるようにした
4. **検証が落ちたら3種類に分ける。** 今回は「抽出漏れ（ハーネス）」が3件、
   「設計変更で古くなったアサーション（製品が正しい）」が2件。**製品の退行は0件。**
   落ちたテストを消して通すのが一番危ないので、どちらかを必ず言い切ってから直す
5. **確定させる瞬間を決める。** 設定を参照し続けると、承認済みの月の目標が後から動く。
   承認時に書き込んで固定する、という一点を決めれば「いつの目標だったのか」が揺れない

---

## ビルド手順（厳守）

1. Python で透過的置換。`open(newline='')` で CRLF 保持、アンカーは `assert count==1`
2. Node `new Function()` で構文チェック（先頭 `<script>` を抽出）
   - `MINUTES_SEED` / `HANDBOOK_SEED` を `grep -v` で落とすと構文が壊れる。**フィルタせず検査する**
3. `APP_VERSION` と `SW_BUILD` を**同時に**上げる
4. lone LF = 0 を確認
5. `/mnt/user-data/outputs/` へコピーして `present_files`
6. 危険な処理は実関数を切り出して Node でモック実行し、**修正前の症状を再現するテストを必ず入れる**
7. 大きな新規ブロックは別ファイル（LF）で作り、CRLFに変換してから差し込む

**Motoさんの開発方針：「憶測で修正しない」。根本原因を確認 → 設計 → 確認 → 実装を1ステップずつ。**

---

## 主要な定数・環境

- ロール/グレード：crew=G1, g2=G2, sl=G3, g4=G4, **chef=G4（Corporate Chef）**, am=G5, gm=G6
  - `ROLE_ALIAS = {g2:'sl', g4:'am'}` はログイン権限のエイリアスのみ。実際の `e.role` は `g2`/`g4` のまま
  - `empGrade()` は `Math.max(titleGrade, roleGrade)` の下限
- 本部のメニュー：CEO/GM = `OFFICE_GROUPS`／経理 = `ACCT_GROUPS`／Chef = `CHEF_GROUPS`
- 店舗：ToriTon(F01) / Tenkichi(F02) / FSP(F03) / Waikiki Garlic Shack(F03-G) /
  Totoya Kaimuki(F04-K)・Piikoi(F04-P)・Aiea(F04-A) / Marujuu(F05)
- Kaimuki / Piikoi のみスキル**7軸・Lv0〜4**（他店は Lv0〜3）
- Corporate Chef の目標：`ccScoreView()` が唯一の読み出し口。**生の `ccScore()` を表示に使わない**
- 保存層：`ls` / `lsSet` / `lsKeys` / `_lsStoreGet` / `_lsRaw` / `_lsRawStr` / `_lsRawDel` / `_lsWriteVerified`
  - **新しい保存経路（`localStorage`/`IndexedDB`/`fetch` の直接呼び出し）を作らない**
  - 自動pruneは `_lsPruneNeeded()`（= `_idbState==='unavailable'`）でしか走らない（v773）
- Supabase: `tgbhgxzehzeouopklhje` / Edge Function: `hyper-worker`, `auth-pin`, `emp-private`, `ob-private`
- GitHub: `motoi107/funergy-growth-os` → `funergy-plus.com`

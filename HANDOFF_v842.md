# Funergy Growth OS — 引き継ぎ v842

**v842：実画面（v841・全8店）のフィードバック反映。**

Moto さんが本番で見た画面から出た指摘に対応した。**不具合1件を含む。**

---

## 0. 最初にやること

```bash
unzip funergy_v842_handoff.zip
tar -xJf handoff_v842.tar.xz
cd handoff_v842
TZ=Pacific/Honolulu node run_verify.js
node make_preview.js        # → preview.html
```

**期待値**

```
合計 PASS=3659  FAIL=0  実行=66/81
合流ルールの未定義: 1        ← spl_skills_st_ の1件だけが既知
同期対象で未保護: 0
予算の対象月: 3 / 7
APP_VERSION=842 / SW_BUILD=842
CRLF=57423 / lone LF=0
```

**v836〜v842 とも未 push。**GitHub は md5 で突き合わせること（引き継ぎの記述を信じない）。

---

## 1. 実画面で分かったこと（重要）

**本番では 人件費率・原価率・営業利益・客数・客単価 が全8店で「判定不可」だった。**
原因はコードではなく**設定**：

| KPI | 目標の出どころ | 状態 |
|---|---|---|
| 人件費率 | `getStoreKpiTargets().laborPct` | **未設定** |
| 原価率 | `getStoreKpiTargets().foodCost` | **未設定** |
| 営業利益・客数・客単価 | `hqPlanForYm()`（本部予算） | **未登録** |

売上だけが予算から判定できていた（4/8 店が未達）。

**→ v842 では「何を設定すれば判定できるか」を画面に出すようにした。**
KPI管理 → 店舗タブで 人件費率・原価率の目標を入れれば、その場で判定が出る。

---

## 2. v842 でやったこと

### ① 不具合修正：「全店で達成」と「判定不可 8店」が同時に出ていた

```js
(missN ? '未達：' + names : '全店で達成')   ← 未達が0なら達成扱いで文字を出していた
+ (unkN ? '判定不可 ' + unkN + '店' : '')
```

達成0・未達0・判定不可8 のときに「全店で達成」と表示していた。
グラフ化に伴いこの文言そのものを廃止し、**達成 / 未達 / 判定不可 の3つの数を必ず並べて出す**形にした。

### ② 判定不可でも実測値を出す（参考値）

目標が無いだけで実測は出せることが多い。
判定層に **`ref`（参考値）** を足した。

- **`actual` は null のまま。**達成／未達に化けさせない（v837 の決めごとを守る）
- `ref` は「参考」と明示して表示する
- 判定できているときは `ref` は `null`（参考値を混ぜない）

これで店舗別の表が「—」だらけにならず、人件費率 26.4%（参考）のように読める。

### ③ 重要KPIを全店1本のグラフに

店舗数のカードを6枚並べる形をやめ、**KPIごとに1行の積み上げ帯**にした。

```
売上      [████████░░░░░░]  4 / 4 / 0     ← 達成 / 未達 / 判定不可
人件費率  [░░░░░░░░░░░░░░]  0 / 0 / 8
```

- 緑＝達成、赤＝未達、グレー＝判定不可。凡例つき
- **行を選ぶと KPI詳細（店舗別の内訳）が開く** ← 「KPI詳細まだ見れない」への対応
- 1店舗のとき（Store Manager）は今までどおり実績/目標のカード

### ④ KPI詳細（店舗別の内訳）

KPIの行を開くと、その KPI の店舗別の表が出る。

- 未達を先に並べる（順位ではない）
- 判定不可の店舗は参考値と理由
- 店舗を選ぶと予実管理へ
- 下に「KPI目標が未設定 6店 — KPI管理 → 店舗タブで…」と直し方

### ⑤ 判定できていない理由カード

表示範囲ぶんの判定不可を**理由でまとめて**出す。件数・対象KPI・店舗数・**直し方**・その画面へ行くボタン。

```
6  KPI目標が未設定
   人件費率・原価率（3店）
   KPI管理 → 店舗タブで 人件費率・原価率の目標を入れる      [ひらく]
```

`MD_FIX` に理由 → 行き先の対応を持たせてある。理由を増やすときはここも足す。

### ⑥ 今日の売上・当日の状況

- **今日の売上を上帯のタイルから独立カードへ戻した。**
  Funergy Now は店舗×ランチ/ディナー/テイクアウトの内訳が入るため、1タイルには収まらない
- **上帯から「全体判定」タイルを外した。**すぐ上のバナーと重複していた
- **当日の状況カードは削除。**上帯と重複していた。
  `mdTodayHtml` は関数ごと消した（`labor_hours_` と同じ「誰も読まない」形を作らないため）

上帯は5枠になった：本日時点の進捗／月末の着地予測／要対応／承認・タスク・エラー／データの入り

---

## 3. 進み具合

| 版 | 内容 | 状態 |
|---|---|---|
| v837〜v841 | 判定層・必須KPI設定・器・統合・フル幅 | 完了 |
| v842 | **実画面フィードバック反映（参考値・KPIグラフ・KPI詳細）** | **完了** |
| v843 | 役職別の作り込み（Head Chef の原価・仕入中心 ／ CEO の絞り込み） | 次 |
| v844 | Work Center の本統合 ＋ 管理統括の「整備中」を外す | |
| v845 | 店長通知の画面連結 | |

---

## 4. 未確認・保留

### ① まず設定を入れてほしい（v842 の効果はここで出る）

- **KPI管理 → 店舗タブ → 人件費率・原価率の目標**（全8店）
- **本部予算（客数×客単価・営業利益）** … `hqPlanForYm` が読む
- 入れた瞬間に、判定不可が達成／未達に変わる

### ② 管理統括

- **`management_control_v815.sql` の流し直しが未実施**（v824 の chef 追加ぶん）
- 管理統括はまだ「整備中」表示中。v844 で外す
- **7月（前月）の検知結果を Moto さんがまだ見ていない**

### ③ 昨対（2025年データ）

「枠だけ作り判定不可表示」で確定。入れるなら `doRangeImport` で 2025年を引けるか実機確認から。
入ったら `MD_KPI.yoy_sales` の `pending` を外す。

### ④ その他

- `_LS_PRUNABLE` が死んでいる（v836 で発見・未着手。掃除時は地雷リストを見る）
- Tip入力権限の調査 / AM の管轄割り当ての実データ確認
- `supabase/functions/` が未push、`hyper-worker` が未デプロイ
- **v839 の出どころ**（並行作業のもの。`HANDOFF_v840.md` 第3章）

---

## 5. 教訓

v807 ①〜⑤、v825 ⑥〜⑫、v835 ⑬〜⑱、v836 ⑲〜㉑、v837 ㉒〜㉔、v838 ㉕㉖、v840 ㉗、v841 ㉘ は有効。

### ㉙ 「Aでなければ B」の二択にすると、Cのときに嘘をつく

```js
missN ? '未達：' + names : '全店で達成'
```

未達が0なら達成、という書き方だった。**判定不可というCがあるので嘘になる。**
3値（達成／未達／判定不可）を扱うコードで `? :` の二択を書いたら、
**必ず3つ目がどちらに落ちるかを確かめること。**

実画面を見るまで気づかなかった。サンプルデータは全部の値が揃っていたため、
Cのケースが1度も出ていなかった。**検証のサンプルに「全部が判定不可」を必ず混ぜる。**

### ㉖ を仕組みで潰した（4回目でようやく）

置換文字列の素の `\n` で `lone LF` を出すのを v838・v840・v842 と3回やった。
注意では直らないので、**`rep()` の中で置換文字列を必ず CRLF に寄せる**ようにした。

```python
def rep(s, old, new, why):
    n = s.count(old); assert n == 1, ...
    new = new.replace('\r\n','\n').replace('\n','\r\n')   # ← 追加
    return s.replace(old, new)
```

さらに書き出し前に `assert lone LF == 0` を置いた。
**同じ失敗を3回したら、注意ではなく仕組みを変える。**

### ㉚ 判定できない理由は、理由だけでは足りない

「目標が未設定」と出しても、どこで設定するのか分からなければ動けない。
`MD_FIX` に **理由 → 直す画面 → 手順** を持たせ、その画面へ飛ぶボタンを付けた。

**エラーや判定不可を出すときは、次の一手まで書く。**

---

## 6. 重要な設計上の決定（勝手に変えないこと）

- **数字の計算を新しく作らない。**コンソールは既存を呼ぶだけ
- **判定不可は 0 ではない。**`actual` は `null`、帯も付けない。
  **実測は `ref`（参考値）として別に持ち、「参考」と明示して出す**
- **未達ゼロでも判定不可が残っていれば「達成」と言い切らない**
- **3値を扱うところで二択の `? :` を書かない**（教訓㉙）
- **率の平均を画面ごとに定義しない。**複数店舗は店舗数で数える
- **ランキングを作らない。**順位番号も付けない
- **コンソールは読むだけ。**承認・タスク・エラーの状態をここで書き換えない
- 承認・タスクは**表示範囲で絞る**（AM が管轄外を見ない）
- **640px以下の既存CSSに触らない。**コンソールの CSS は `.md-*` と `body.md-wide` だけ
- **呼ばれなくなった関数は消す**（`labor_hours_` と同じ形を作らない）

---

## 7. 同梱ファイル（追加・変更ぶん）

| ファイル | 用途 |
|---|---|
| `index.html` / `sw.js` | **v842 本体（GitHub 未 push）** |
| `preview.html` / `make_preview.js` | 画面プレビュー。一部の店舗をわざと目標未設定にして判定不可の出方も見せている |
| `verify_v842.js` | **新設**。参考値・KPIグラフ・KPI詳細・理由の検証（PASS=95） |
| `index_v842_backup.html` | **新設**。次版の検証が「修正前」として使う |
| `DESIGN_management_dashboard.md` | 仕様16章 → 既存コードの対応表 |
| `HANDOFF_v841.md` 〜 `HANDOFF_v807.md` | 過去の引き継ぎ |

---

## 8. 次のセッションの入り方

1. `TZ=Pacific/Honolulu node run_verify.js` → 第0章の期待値と一致するか
2. GitHub main を md5 で突き合わせる
3. `node make_preview.js` で今の画面を見る
4. **Moto さんに KPI目標と本部予算を入れてもらう**（判定不可がほどける）
5. v843（役職別の作り込み）から進める

**Motoさんは実画面を見て的確に指摘する。**v842 の不具合も実画面で見つかった。
作り込む前にプレビューを見てもらい、本番に上げたら早めに画面を確認すること。

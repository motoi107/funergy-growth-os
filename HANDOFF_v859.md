# Funergy Growth OS — 引き継ぎ v859

**v859：管理コンソールの集計キャッシュ。読み取りが94%減る。**
**（店舗カードUI = v860 の土台。計算式は1行も変えていない）**

---

## v859：同じ店の同じ月を16回ずつ計算し直していた

Motoさんから「軽くする提案を」との依頼。**推測せず、呼び出し回数を実測した。**

### 実測：管理コンソール1回の描画（9店 × 5KPI）

| 呼び出し元 | `mdVerdict` |
|---|---|
| 判定バナー（`mdScopeVerdict` → `mdStoreVerdict` × 9店 × primaries 5） | 45 |
| 重要KPI（`mdKpiScopeRoll` × 5 → `mdKpiRoll` × 9店） | 45 |
| 判定できていない理由（`mdReasonRoll` × 9店 × 5KPI） | 45 |
| KPI行を1つ開く | 9 |
| **合計** | **144** |

`mdVerdict` は毎回 `cumulativeToToday` → `laborCoreForYm` を呼ぶ。
`laborCoreForYm` は **1日1キーの `getTipLabor`** を引くので、

```
getTipLabor  9店 × 31日 × 16回 ＝ 4,464回 / 1描画
```

### 直し方：描画1回ぶんだけ生きるキャッシュ

```js
var _mdCache = {}, _mdCacheArmed = false;
function _mdMemo(ns, key, make){
  if(!_mdCacheArmed){
    _mdCacheArmed = true;
    try{ setTimeout(mdCacheClear, 0); }catch(e){ _mdCacheArmed = false; return make(); }
  }
  ...
}
```

**★ 呼び出しスタックが空になった瞬間に捨てる。**
`setTimeout(0)` を最初の1回だけ仕掛けるので、キャッシュは今の同期処理の中でしか生きない。
**描画をまたがないので、保存のあとに古い値が残ることが構造的に起きない。**
`renderPage` の先頭でも明示的に捨てる（二重の保険）。

| | 前 | v859 |
|---|---|---|
| `cumulativeToToday` | 144回 | **9回** |
| `getTipLabor` | 4,464回 | **279回（94%減）** |

**計算式は1行も変えていない。**元の本体は `_cumulativeToTodayRaw` / `_laborCoreForYmRaw` / `_monthStatsRaw`
としてそのまま残し、包みは `_mdMemo` を呼ぶだけ。
検証で「本体が前版と完全に同じ」ことを突き合わせている。

キャッシュしない場合も明示してある。

- `laborCoreForYm` の `opts.endBefore` / `opts.weekSun` 付き … 結果が変わるので毎回計算
- `cumulativeToToday` の引数なし … 中の分岐（`daysOfYm` / `monthDays`）が変わるので鍵を分ける

返り値を書き換えている箇所が無いことも確認済み（書き換えるとキャッシュが汚れる）。検証で見張っている。

### ⚠️ このとき既存の検証が3本落ちた（自分の手順ミス）

`verify_v793` / `v802` / `v805` が落ちた。**製品ではなくハーネスの問題だった。**
この3本は `index.html` を直に見て「賃金計算のコアは1文字も変えていない」を守っており、
本体の名前が `_..._Raw` へ移ったことで**包みのほうを掴んでいた**。

意図（コアを変えない）は保ったまま、本体を見るように直した。

```js
function coreOf(text, name){
  const raw = '_' + name + 'Raw';
  return text.indexOf('function ' + raw + '(') >= 0 ? grab(text, raw) : grab(text, name);
}
```

**引き継ぎに「定義を消す前に `grep -l '<名前>' verify_*.js` を打つ」と書いてあるのに、打たなかった。**
名前を「消す」だけでなく「移す」ときも同じ。

### 教訓 71 関数の名前を移すときも、先に検証を grep する

消すときだけの手順だと思っていた。**移す・包む・改名も同じ**。
`index.html` を直に見る検証（コア保護系）は、名前の移動で必ず落ちる。

### 教訓 72 キャッシュは「いつ捨てるか」を先に決める

`(店舗, 年月)` で覚えるところまでは誰でも書ける。事故は無効化で起きる。
**「呼び出しスタックが空になったら捨てる」なら、無効化の判断が要らない。**
保存経路を1つ見落としても古い値が残らない。効果は落ちない（144回は1つの同期処理の中で起きるため）。

---

## 次（v860）でやること

**店舗カード＋グラフ**。Motoさん確認済みの仕様：

| | 決定 |
|---|---|
| 表示内容 | 実績 ／ 目標との差 ／ 前週比（月次進捗のKPIと同じ計算・同じ表示） |
| ① 今の「KPI別」 | **なし**（カードをタップして詳細） |
| ② 目標 | 月次進捗のKPIと連動（同じ表示） |
| ③ 判定不可 | 実測を「参考」として出し、下に行き先を小さく。判定色は付けない |
| ④ AM | 管轄店舗のみ |
| グラフ | **案C＝売上の累計線＋週×KPIヒートマップ** |
| 向き | 人件費率・原価率は「目標÷実績」で反転。**常に緑＝良い** |
| 週の区切り | **`laborMonthWeeks(ym)`（日曜始まり）**。人件費・OTと同じ週 |
| G4 | 1店のときはカードを並べず単店レイアウト（`stores.length === 1` で分岐） |

### ⚠️ v860 で決めが要る1点：週次の人件費率

`laborCoreForYm` は**すでに週ごとにループしている**（OTの40時間判定のため）ので、
週次バケットを足しても**追加の読み取りはゼロ**。ただし人件費「額」は週ループの外で
`byName`（人ごとの月合計）から `laborForEmployee` で出している。

方針は **「同じ計算を週で束ねる」**。新しい式は作らない。

- 時給者 … 週の `reg`/`ot` に対して**同じ `laborForEmployee`** を呼ぶ（OTは元から週単位なので厳密に一致）
- 月給・保険 … `storeSalaryDaily` / `storeInsuranceDaily` を**その週の在月日数ぶん足す**（日割りの合計＝月額）

**検証で Σ週 ＝ 月 を突き合わせる。**ここが合わなければ実装が間違い、と分かるようにする。

---

# （以下 v858 までの内容）

# Funergy Growth OS — 引き継ぎ v858

**v858：客数は「進捗ぶんの予算」と比べる。月間総額と比べていたので月末まで必ず未達だった。**

---

## v858：客数がずっと未達になっていた

v857 で客数の目標を予算設定から読むようにしたが、**月間の客数予算をそのまま目標にしていた。**
客数は月をまたいで積み上がる累計なので、月間総額と比べると月末まで必ず未達になる。

**同じ計算がアプリ内に既にあった。**

```js
/* mcDetectGuests */
var pace = c.budToToday / c.budMonth;   /* 進捗率＝売上予算の進み具合 */
var expG = budG * pace;                  /* 進捗ぶんの客数予算 */
var budSpend = c.budMonth / budG;        /* 客単価予算（按分しない） */
```

表示ラベルもそう分かれていた。

```js
guests_short: { cur: '客数',   tgt: '進捗ぶんの予算' },
spend_short:  { cur: '客単価', tgt: '予算' },
```

**同じ式に揃えた。**売上も最初から `budToToday` と比べている。

| | 前 | v858 |
|---|---|---|
| 客数 | 月間客数予算 | **月間客数予算 × 進捗率** |
| 客単価 | 売上予算÷客数予算 | 変更なし（1人あたりの平均なので按分しない） |
| 実績の未入力日 | 注記なし | **「実績が N 日ぶん未入力」**（売上と同じ） |

8月4日時点（進捗12.9%）で目標は 2,350 → **303.21**。月末には 2,350 に一致する。

### 教訓 69 累計の指標は、必ず「進捗ぶん」と比べる

売上は最初から `budToToday` と比べていた。客数も同じ性質なのに、月間総額と比べていた。
**「月末にならないと達成できない目標」は、目標として機能しない。**

累計（客数・売上・丼数）は按分する。率（客単価・原価率・人件費率）は按分しない。

### 教訓 70 既存の同じ計算を探してから書く

pace / expG / budSpend は `mcDetectGuests` に全部あった。自分で式を起こす前に探すこと。
式が2か所に分かれると、片方だけ直したときに必ず食い違う。

### ⚠️ 次に見るべきこと（未確認）

客数予算の集計範囲が2か所で違う。

- `monthGuestBudget()` … lunch + dinner（ダッシュボードの判定で使用）
- `mcDetectGuests` … lunch + dinner + **takeout**

Totoya のようにテイクアウト比率が高い店で数字がずれる可能性がある。
実績側（`monthStats().guests`）がテイクアウトを含むかどうかと合わせて、Motoさんに確認すること。

---

# （以下 v857 までの内容）

# Funergy Growth OS — 引き継ぎ v857

**v857：客数・客単価の目標は「予算設定」から読む。v853〜v856 の前提が誤りだった。**

---

## v857：4版にわたる思い違いの訂正

Motoさんの指摘：「7月分はセキュアページで保存していない。予算設定したものが反映されていると思います」。
**そのとおりだった。コード自身がそう書いている。**

```
1419行   ※ 集客数・客単価はKPI目標から削除（客数/客単価は各月の予算設定から反映）。
56623行  集客数の目標は予実（曜日別の客数予算×月の曜日回数）と連動。予実で設定してください。
```

予実の他の画面（KPIタイル・KPI推移一覧・予実サマリー）は**すべて `monthGuestBudget()` を使っている。**
客単価予算は `keyKpiStats` の `ppaBudget` ＝ **売上予算 ÷ 客数予算**。

**ダッシュボードの判定（`mdVerdict`）だけが本部予算（`hqPlanForYm`）を見ていた。**そこが唯一の食い違い。

### 何を間違えたか

v853 で「本部予算から来る」と読んだ。読んだ場所は合っていたが、**それが仕様なのかバグなのかを確かめなかった。**
そのうえに v855（案内先）・v856（保存の仕組み）を積み上げた。
**土台が間違っていたので、上に積んだものは全部無駄だった。**

Motoさんは4回、間違った場所へ行かされた。

### 直したこと

```js
var gb = monthGuestBudget(storeId, _off);                      /* 曜日別の客数予算×曜日回数 */
var tg = (code === 'guests') ? gb
       : ((gb > 0 && c.budMonth > 0) ? c.budMonth / gb : 0);   /* 客単価＝売上予算÷客数予算 */
if (!(tg > 0)) return mdUnknown(code, 'no_budget', '曜日別の客数が未入力', av);
```

判定不可のときの案内も **予実**（`page:'budget'`）に変わる。

**営業利益（profit）は今までどおり本部予算**。あちらは年間計画の利益なので `hqPlanForYm` が正しい。

### あわせて：本部予算の保存が黙って消していた

客数・客単価が空だと `sales` が null になり、**保存ではなく削除**が走るのに「保存しました」と出ていた。
`hasDetail` に原価率・人件費率が入っていないので、率だけ入れて保存しても消えた。
空の年月（`2026-08: {}`）が残っていたのがその跡。

- 売上が出せないときは**保存せず理由を出す**
- 既にある月を消すときは**確認を取る**
- 空の年月を残さない

### 教訓 66 「そう書いてある」と「そういう仕様だ」は別

`mdVerdict` が `hqPlanForYm` を読んでいるのは事実だった。だが**それが正しいとは限らなかった。**
同じ指標を出している画面が他に3つあり、そちらは別の出どころを使っていた。

**ある値の出どころを直すときは、同じ値を出している他の画面を先に数える。**
1か所だけ違うなら、その1か所がバグ。

### 教訓 67 使う人の「そんな操作はしていない」を最優先で聞く

Motoさんは「セキュアページで保存していない」と言った。データにはセキュアページ経由の記録があった。
**矛盾したときに疑うべきはデータの解釈のほうで、使う人の記憶ではなかった。**
実際、7月分は予算設定から入ったもので、こちらの読み方が間違っていた。

### 教訓 68 検証でコード本体を見るときは、必ずコメントを外す

v855・v857 と2回、**自分が書いた説明コメントに含まれる関数名を数えて**検証が落ちた。
`grab(...).replace(/\/\*[\s\S]*?\*\//g, '')` を通してから判定する。

---

# （以下 v856 までの内容）

# Funergy Growth OS — 引き継ぎ v856

**v856：セキュアページの本部予算が保存されない件を修正。監査の盲点も塞いだ。**

---

## v856：保存が消えていた理由（2つ重なっていた）

### ① セキュアページでは押し上げが黙って落ちていた

`showManagerSecure()` へ来る経路（ログイン画面 → メールで管理者ログイン）は **`initApp()` を通らない**。
`_supaBooting = false` は `initApp()` の中にしかないので、**セキュアページでは `_supaBooting` が true のまま**だった。

```js
async function syncMasterToSupabase(key, val){
  if (_supaBooting) return;   // ← ここで黙って落ちる
```

つまり**セキュアページでの保存（本部予算・簡易PL）は、一度もクラウドへ上がっていなかった。**

→ `await supaPullAllMasters()` の**あとに** `_supaBooting = false` を入れた。
先に解除すると pull 中の書き込みを押し上げてしまうので、順番が大事。

### ② hq_budget に合流ルールが無かった

`hq_budget` は `OP_SYNC_KEYS` に居るのに合流ルールが無かった。

```js
var _def = _opMergeDef(row.key);        // ← hq_budget は null
if(_def){ ... }                          // ← 通らない
_lsWriteVerified(row.key, JSON.stringify(row.value));   // ← クラウドの値で丸ごと上書き
```

`showManagerSecure()` は**開くたびに `supaPullAllMasters()` を呼ぶ**。
①で押し上がっていないので、次に開くと古いクラウドの値で上書きされ、**保存が消えた**。

→ `mergeHqBudget` を作って登録した。`{ 年月: { 店舗: {..., at } } }` の2段マップを、
**年月ごと・店舗ごとに保存時刻の新しいほうを採る**。他の月・他の店を巻き込まない。

## ⚠️ 監査に大きな穴があった

`audit_merge_coverage.js` は **`LS_NEVER_FREE` と `OP_SYNC_PREFIX`（接頭辞）しか見ていなかった。**
**`OP_SYNC_KEYS`（完全一致のキー一覧）は1件も見ていなかった。**

だから「同期対象で未保護: 0」と出ていたのに、実際には**61件が丸ごと上書きされ得る**状態だった。
`hq_budget` はその1つ。

第4部を足し、`run_verify.js` の基準値に `OP_SYNC_KEYS で未保護: 61` を追加した。
**これは「今わかっている借金」。**塞ぐたびに下げる。増えたら新しい穴。

とくに危ないもの（人が手で入れる／複数端末で触る）：
`pl_config` `pl_actual` `pl_overrides` `hq_actions` `approval_routing` `store_fixed_costs`
`evaluations` `meetings` `skills` `lss_scores` `mileage_config` `order_lists` `trainees`
`shift_sections` `shift_headcount` `shift_postime` `shift_secrole` `shift_ld_head`

### 教訓 64 監査は「何を見ていないか」を書いておく

第2部の見出しは「クラウド同期の対象すべて」と書いてあったが、実際は接頭辞だけだった。
**見出しの言葉が実装より広いと、0という数字が嘘の安心になる。**

監査を書くときは、対象範囲を**列挙元の変数名で**書く。「すべて」と書かない。

### 教訓 65 初期化フラグは、通らない入口がないか数える

`_supaBooting` を下ろすのは `initApp()` の1か所だけだった。
だが画面へ入る経路は2つあり、片方（管理者セキュアページ）は `initApp()` を通らない。

**「起動が終わったら下ろす」フラグは、起動の経路が1本だと決めつけない。**

---

# （以下 v855 までの内容）

# Funergy Growth OS — 引き継ぎ v855

**v855：本部予算の案内先を、実在する道順に直す。v853 の案内は間違っていた。**

---

## v855：また間違った行き先を出していた

v853 で「客数・客単価の目標は本部予算から来る」と突き止めたのは正しかった。
**だが案内先を確かめずに書いたため、また存在しない道を出していた。**

> 誤：**成長管理 → 本部予算タブ**

`growth_mgr`（成長管理）は `renderGrowthManager()` ＝ **スタッフ育成の画面**で、本部予算は無い。
Motoさんが「成長管理タブ内にも見当たらない」と指摘されたとおり。

### 実際の道順（呼び出しを1本ずつ追った）

```
renderHqBudget()        ← 呼ぶのは renderSecureBody() の1か所だけ
renderSecureBody()      ← 入るのは #mgr-secure-body だけ
#mgr-secure-body        ← あるのは #manager-secure-screen（管理者セキュアページ）だけ
#manager-secure-screen  ← 開くのは showManagerSecure() だけ
showManagerSecure()     ← 呼ぶのは doManagerLogin()（メール認証）だけ
入口 #mgr-login-entry   ← ログイン画面のPIN入力段。しかも ceo/gm/office にだけ表示
```

**アプリ内のページではないので `renderPage()` では行けない。**
`page` を空にして、道順そのものを出すようにした。

> **ログアウト → 自分を選んでPIN画面の「メールで管理者ログイン」→ 管理者セキュアページ → 本部予算タブ → 店舗を選び、月ヘッダの✎ で 客数・客単価 を入れる**

`hq_budget` を書くのはセキュアページの2か所だけで、**他に入力口は無い**ことも確認済み。

### 教訓 63 案内先は、呼び出しを1本ずつ最後まで追ってから書く

v853 で「タブ名の一覧」を見て `成長管理` と推測した。タブ名は合っていたが、**それを載せている画面が違った。**
v842 の教訓（行き先まで出す）→ v853 の教訓61（正しい一手でなければ意味がない）に続いて、**3回目の同じ失敗。**

**画面名を書くときは、その画面へ実際に到達する経路を関数呼び出しで確かめる。**
「そういう名前のタブがある」は、そこへ行けることを意味しない。

---

# （以下 v854 までの内容）

# Funergy Growth OS — 引き継ぎ v854

**v854：締めの検知は、営業が終わってから出す。**

---

## v854：営業中にエラーが出ていた（実機で発覚）

キャッシュチップ未入力のエラーが、**その日の営業が終わる前**に出ていた。
当日ぶんを入れて消しても翌日の昼にまた出るので、**「対応しても戻ってくる」**ように見えていた。

原因は日付の数え方。

```js
function _ecRecentDates(n){ var out=[], d=new Date(); ... }   // ← 今日から数える
```

エラーセンターの2つの検知が当日を含んでいた。
**タスク側（`checkCashTipTasks`）はもともと前日を見ており、そこだけ食い違っていた。**

| | 前 | v854 |
|---|---|---|
| キャッシュチップ未入力（14日） | 当日を含む | **前日から** |
| 退勤打刻漏れ（7日） | 当日を含む | **前日から** |
| Store CMD・日報・振り分け | 選んだ日 | **変更なし** |

打刻漏れも同じ穴だった。**まだ勤務中の人が「退勤打刻漏れ」に出ていた。**同時に直した。

日付を選んで見る画面（Store CMD のチップ管理・日報・チップ振り分け）は**そのまま**にした。
あそこは「今日の締めで入れてください」と促す場所なので、翌日にすると締めの入口が無くなる。

### ついでに直した：ランチ休の日にタスクだけ残る

タスクの自動完了は `rec.entered`（L/D 両方の入力）を条件にしていた。
だが `cashTipMissing()` は**ランチ休なら L 未入力でも未入力扱いにしない**。
そのためランチ休の日は、エラーは消えるのに**タスクだけ「進行中」で残っていた**。

自動完了の条件を `!cashTipMissing(storeId, date)` にそろえた。エラーとタスクが必ず一致する。

### 教訓 62 「締め」を見る検知は、締めが終わってから走らせる

未入力・打刻漏れ・未確定のような「その日の作業が終わったか」を見る検知は、
**当日に出しても直しようがない。**出た側は毎日ノイズとして扱うようになる。

**同じことを見ている別の仕組みがあるなら、日付の起点をそろえる。**
タスクは前日、エラーは当日、と食い違っていたのが今回の原因だった。

---

# （以下 v853 までの内容）

# Funergy Growth OS — 引き継ぎ v853

**v853：客数・客単価・営業利益の「判定不可」が、間違った画面へ案内していたのを修正。**

---

## v853：案内先が間違っていた（実機で発覚）

ダッシュボードの重要KPIで 客数・客単価 が「KPI目標が未設定」と出る。
案内どおり **KPI管理 → 店舗タブ**に目標を入れても、ずっと判定不可のままだった。

**原因は案内先の間違い。**

| KPI | 目標の出どころ |
|---|---|
| 人件費率・原価率 | `getStoreKpiTargets()` ← **KPI管理** |
| **客数・客単価・営業利益** | `hqPlanForYm()` ← **本部予算**（`hq_budget`） |

この3つにも理由コード `no_target` を共用していたため、
`MD_FIX.no_target` の「KPI管理 → 店舗タブで 人件費率・原価率の目標を入れる」が出ていた。
**指示どおりに操作しても直らない＝行き止まりの案内**になっていた。

理由コードを分けた。

```js
no_hq_plan: { label: '本部予算が未設定', page: 'growth_mgr',
              how: '成長管理 → 本部予算タブ → 店舗を選び、月ヘッダの✎ で 客数・客単価 を入れる' },
```

**正しい入れ方**：成長管理 → 本部予算タブ → 店舗を選ぶ → 月ヘッダの ✎ → 客数・客単価。
売上は 客数×客単価 で自動計算される。1月ぶん入れたら「今年の他の全月にコピー」が使える。

判定そのもの（数字・達成の向き）は1つも変えていない。前版と突き合わせて確認済み。

### 教訓 61 「次の一手」は、正しい一手でなければ意味がない

v842 で「判定不可には行き先まで出す」を入れた。それ自体は正しかった。
だが `no_target` を5つのKPIで共用したため、**3つには間違った行き先**を出していた。
Motoさんは案内どおり操作し、変わらないので詰まった。

**理由コードを共用するときは、行き先まで同じかを確かめる。**
札を1つ増やすほうが、間違った案内を出し続けるより安い。

---

# （以下 v852 までの内容）

# Funergy Growth OS — 引き継ぎ v852

**v852：名前をキーにした同期キー（固定シフト／シフト最適化の個人設定）にテスト用スタッフを入れない。**
**あわせて v851 の不具合（開けない画面へ飛ばされる）を修正。**

---

## 0. ⚠️ 毎回のリマインド（Motoさん依頼）

**公開URLで配布しているため Supabase の anon キーがクライアントに露出しています。**
`tip_labor` の RLS ポリシー `tip_labor_anon_all` は anon に **ALL（qual=true / with_check=true）** を与えており、
**URLとソースを見た人なら誰でもチップ・勤怠データを読み書きできる**状態です。

恒久対策は「読みは anon／書きは認証済みのみ」または Edge Function 経由。今回も未着手です。

---

## 0.5 現在地（ひと目で）

| | 状態 |
|---|---|
| 現行版 | **v859**。**v849〜v859 が GitHub 未 push**（v848 までは push 済み） |
| 検証 | `PASS=4801 / FAIL=0 / 実行=83/98`。検証98本・バックアップ78件 |
| クラウドの状態 | `employees` は **0件 / 全152件**（掃除済み・本物は無傷） |
| **未確認** | **`shiftopt_mem_` に TEST が戻っていないか**（check SQL の③） |
| **Motoさん待ち①** | **v852 を先に入れてから**、③を確認 → 出たら cleanup を再実行 |
| **Motoさん待ち②** | ガソリン代の単価を $0.76 に（IRS 2026/7/1〜。現在 $0.70） |
| **Motoさん待ち③** | **予実で曜日別の客数予算**を入れる（客数・客単価の判定に必要）。本部予算は営業利益の判定にだけ必要 |
| **Motoさん待ち④** | `management_control_v815.sql` の流し直し（v824 の chef 追加ぶん） |
| 積み残し（大） | **公開URL／anonキー**（第0章）・管理統括の「整備中」・昨対の2025年データ |

---

## 1. 最初にやること

```bash
unzip funergy_v859_handoff.zip
tar -xJf handoff_v859.tar.xz
cd handoff_v859
TZ=Pacific/Honolulu node run_verify.js
```

**期待値**

```
合計 PASS=4801  FAIL=0  実行=83/98
合流ルールの未定義: 1
同期対象で未保護: 0
OP_SYNC_KEYS で未保護: 61
予算の対象月: 3 / 7
APP_VERSION=859 / SW_BUILD=859
CRLF=58232 / lone LF=0
```

---

## 2. ⚠️ 実データで見つかった穴（v850 では足りなかった）

### 何が起きていたか

掃除SQLを流したあと、`check_test_staff_v850.sql` の③で **`shiftopt_mem_F04-P` に TEST が2件**出ました。
Piikoi でシフト最適化を試したときのものです。

`employees` は v850 で塞いだので0件でしたが、**名前をキーにした同期キーは塞いでいませんでした。**
前の版では「この2つは同期されるので気をつけてください」と**注意書きで済ませていました**。足りませんでした。

### なぜ SQL で消しても戻るのか

```js
'shiftopt_': { merge: mergeMapByTime, ... }

function mergeMapByTime(cloud, local){
  ...
  Object.keys(l).forEach(function(k){
    if(!(k in out)){ out[k]=l[k]; return; }   // ← ローカルにしか無い名前は無条件に足し戻す
```

**キーの和集合**です。さらに `opMergePush()` は合流結果を**ローカルにも書き戻します**。

つまり Piikoi を試した端末にその2名がローカルに残っていれば、
**次にその店の最適化設定を保存した瞬間、クラウドへ足し戻されます。**SQL の物理削除だけでは止まりません。

さらに、**画面から消す手段がありません。**`renderShiftOptConfig` は `staffForStore()` で回すので、
従業員マスターから消えた TEST は一覧に出ず、`shiftopt_mem_` の残骸だけが触れないまま残ります。

### 直し方：v849（スキル）と同じ形

**同期しないキーへ分けました。**

```js
var TEST_NAMEKEY_STORE = 'tst_namekey';   /* 同期対象外 */
function _isTestName(n){ return /^TEST\s/.test(String(n||'')); }
```

| | |
|---|---|
| 読み | `_readNameKeyMap()` … 同期キーから TEST を外し、テスト用キーから重ねて返す |
| 書き | `_writeNameKeyMap()` … 本物は同期キー、TEST はテスト用キーへ振り分ける |
| 後始末 | `purgeTestFromNameKeys()` … 全店の残骸を掃除。`removeTestStaff()` から呼ぶ |

**読み取りでは絶対に保存を書き換えません。**書くと押し上げ→合流→また TEST が戻る、が循環するためです。

**保存のたびに同期キー側から TEST が落ちるので、既存の残骸も自然に消えます。**
本物を1人保存するだけで、その店の残骸がクラウドへ行かなくなります。

対象は2つ。

- `shiftopt_mem_<店>`（`mergeMapByTime` ＝ 和集合。**墓標なし**）
- `shift_fixed_<店>`（`mergeMapStamped` ＝ 和集合。墓標はあるが**墓標にも名前が残る**）

### ⚠️ 順番が大事です

1. **先に v852 を入れる**（特に Piikoi を試した端末）
2. そのあと `check_test_staff_v850.sql` の③で確認
3. まだ出るなら `cleanup_test_staff_v850.sql` を流す

**逆の順番だと、掃除した直後に端末から押し戻されます。**

---

## 3. v851 の不具合を修正

`createTestStaff` / `removeTestStaff` / `setTestSkillLevels` が `renderPage('settings')` を呼んでいました。
v851 でカードをシフト画面へ移したので、**シフト画面から押すと、開けない設定画面へ飛ばされます。**
Head Chef と AM は設定画面のロールに入っていないので、そのまま行き止まりになります。

```js
renderPage((typeof curPage!=='undefined' && curPage) ? curPage : 'shift_admin');
```

**画面を移すときは、そこから呼ばれる関数の遷移先も見る**（教訓58）。

---

## 4. Motoさん待ち：③の確認

`check_test_staff_v850.sql` の**③だけ**を流してください（読み取りのみ）。

```sql
select key,
       (select count(*) from jsonb_object_keys(value) k where k like 'TEST %') as test_names
from public.app_state
where (key like 'shift_fixed_%' or key like 'shiftopt_mem_%')
  and jsonb_typeof(value) = 'object'
  and exists (select 1 from jsonb_object_keys(value) k where k like 'TEST %')
order by key;
```

- **0行** → もう戻っていません。v852 を入れれば以後は入りません
- **また出る** → すでに押し戻されています。v852 を入れてから `cleanup_test_staff_v850.sql` を再実行

なお ① は **0件 / 全152件**で確認済みです。掃除で本物を巻き込んでいません。

---

## 5. 未確認・保留

### ① v849〜v852 の実機確認（最優先）

- シフト作成 → 最適化タブの一番下にカードが出るか（設定画面からは消えています）
- Head Chef / AM で**見えるか**。SL で**見えないか**
- **「15名を作成」を押したあと、シフト画面のままか**（v852 で直したところ）
- Lv タグが出るか。Kaimuki / Piikoi で **Lv が4段**になるか
- 最適化プレビューで **Lv3 の4名が先に**選ばれるか
- **TEST 15 OpsLeader（G3・Lv0）が最後まで**選ばれないか
- メインポジション指定時、**TEST 02 が mainCap 枠に入らない**か

### ② 前の版からの持ち越し

- チップ調整の保存（`tip_credit_adj_v848.sql` 適用済み）。実機確認待ち
- Akane（事務Crew）：承認の拒否文言／ガソリン代の精算が見えるか
- Management Console：**KPI目標と本部予算が未設定のため全8店で判定不可**
- **`management_control_v815.sql` の流し直しが未実施**
- 管理統括はまだ「整備中」表示中
- 昨対（2025年データ）… 枠だけ
- `_LS_PRUNABLE` が死んでいる（v836 で発見・未着手）
- `supabase/functions/` が未push、`hyper-worker` が未デプロイ

---

## 6. 教訓

v807 ①〜⑤、v825 ⑥〜⑫、v835 ⑬〜⑱、v836 ⑲〜㉑、v837 ㉒〜㉔、v838 ㉕㉖、v840 ㉗、
v841 ㉘、v842 ㉙㉚、v843 ㉛㉜、v844 ㉝㉞㉟、v845 ㊱㊲㊳、v846 ㊴㊵㊶、v847 ㊷〜㊼、
v848 ㊽㊾㊿、v849 51〜53、v850/851 54〜57 は有効。追加分：

### 58 「注意書きで済ませた」ものは、いずれ実データで返ってくる

v849 で「`shiftopt_mem_` と `shift_fixed_` は同期されるので気をつけてください」と画面に書いた。
2版あとに、実際に `shiftopt_mem_F04-P` へ2件残っていた。

**注意書きは対策ではない。**人が気をつけ続けることを前提にした設計は、必ず破れる。
書いた時点で「これは塞げないのか」を必ず考える。

### 59 掃除する順番を間違えると、掃除が無効になる

クラウドを SQL で消しても、その名前を持ったままの端末が次に保存すれば戻る。
**先に「出さない」を配ってから、掃除する。**

一般化すると、**和集合で合流するデータは、送り手を止めてからでないと消えない。**

### 60 画面を移したら、そこから呼ばれる関数の遷移先も見る

v851 でカードを設定画面からシフト画面へ移したが、
ボタンの中の `renderPage('settings')` はそのままだった。
**移した先のロールでは開けない画面**へ飛ばしていた。

**移動は置き場所だけの話ではない。**入口を変えたら、出口も揃える。

---

## 7. 重要な設計上の決定（勝手に変えないこと）

- **公開URL／anonキーの件は毎回リマインドする**（第0章）
- **GitHub main は毎回 md5 で突き合わせる。引き継ぎの記述を信じない**（v848 で実際に食い違った）
- **テストデータはクラウドに出さない**（教訓55）。push も pull も塞ぐ
- **和集合で合流するキーには、テストデータを入れない**（教訓58・59）
- **テストデータは作るより先に消し方を作る**（教訓㊷）
- 事務Crew（office_crew）は閲覧のみ。決める操作は `denyViewOnly()` で止める
- 「見える」と「できる」を別のゲートにする
- **DBの列を使うコードを書いたら、同じ版で SQL を同梱し、実際に流して確かめる**
- **外部の基準値は発効日つきの表で持ち、過去は動かさない**（教訓㊿）
- 数字の計算を新しく作らない。判定不可は 0 ではない。ランキングを作らない
- 640px以下の既存CSSに触らない

---

## 8. 適用が必要な SQL

| ファイル | 状態 |
|---|---|
| `check_test_staff_v850.sql` | ①は確認済み（0/152）。**③がまだ** |
| `cleanup_test_staff_v850.sql` | 1回実行済み。**③が出るなら v852 を入れてから再実行** |
| `management_control_v815.sql` | **v824 で chef 追加。流し直しが必要** |
| `tip_credit_adj_v848.sql` | 適用済み（8列 OK・確認済み） |
| `management_control_v808/811/v822_cleanup.sql` | 適用済み |

**v852 で新しく必要な SQL はありません。**

---

## 9. 同梱ファイル（追加・変更ぶん）

| ファイル | 用途 |
|---|---|
| `index.html` / `sw.js` | **v852 本体（GitHub 未 push）** |
| `verify_v850.js` | クラウドへ出さない検証（PASS=52） |
| `verify_v851.js` | G4開放・置き場所・文言の検証（PASS=62） |
| `verify_v852.js` | **新設**。名前キーの分離・後始末・遷移先の検証（PASS=74） |
| `check_test_staff_v850.sql` / `cleanup_test_staff_v850.sql` | クラウドの確認と後始末 |
| `index_v850〜852_backup.html` | 各版のバックアップ |

---

## 10. 作業のしかた（この環境でのメモ）

### ビルド

1. `cp index.html index_v<今の版>_backup.html`（変更**前**に）
2. Python の透過置換。**`rep()` は old / new とも CRLF に寄せてから使う**
3. `APP_VERSION` と `SW_BUILD` を同時に上げる
4. Node の `new Function()` で構文チェック
5. **書き出し前に `assert lone LF == 0`**
6. 前版と差分を取り、意図した範囲だけかを目で見る
7. `TZ=Pacific/Honolulu node run_verify.js`
8. 最後に `cp index.html index_v<新版>_backup.html`

### PostgreSQL を立てる

`apt-get update` が **nodesource のリポジトリで 403 になり失敗**する。先に外すこと。

```bash
mkdir -p /tmp/aptoff && mv /etc/apt/sources.list.d/nodesource.sources /tmp/aptoff/
apt-get update -qq && DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends postgresql
export PGBIN=/usr/lib/postgresql/16/bin
mkdir -p /tmp/pgdata && chown -R postgres /tmp/pgdata
su postgres -c "$PGBIN/initdb -D /tmp/pgdata -U postgres"
su postgres -c "$PGBIN/pg_ctl -D /tmp/pgdata -l /tmp/pg.log -o '-k /tmp -p 5433' start"
su postgres -c "$PGBIN/psql -h /tmp -p 5433 -U postgres --single-transaction -v ON_ERROR_STOP=1 -f your.sql"
```

**`--single-transaction` を使うなら SQL 側に `begin;/commit;` を書かない。**

### この環境から出られる先

`api.github.com` / `raw.githubusercontent.com` / npm / PyPI などは通る。
**Supabase（`*.supabase.co`）は通らない**（`x-deny-reason: host_not_allowed`）。
クラウドの中身を見る必要があるときは、SQL を書いて Motoさんに流してもらう。

**Supabase の SQL Editor は、複数文を流すと最後の結果しか出さない。**
確認用SQLは**1文ずつ流してもらう**か、1文にまとめる。

### 気をつけること

- 検証が落ちたら、**まず検証を疑う**（v851・v852 とも検証側の間違いだった）
- **定義を消す前に `grep -l '<名前>' verify_*.js` を打つ**
- 各 `verify_vNNN.js` は `index_vNNN_backup.html` に固定されている。
  **今の `index.html` を見ているのは最新版の検証だけ**
- **同じコンテナで並行作業が走っていたことがある**（v839）。作業前に `APP_VERSION` と md5 を確認する

---

## 11. 次のセッションの入り方

1. **第0章（公開URL／anonキー）のリマインドを伝える**
2. `TZ=Pacific/Honolulu node run_verify.js` → 第1章の期待値と一致するか
3. **GitHub main を md5 で突き合わせる**（`https://raw.githubusercontent.com/motoi107/funergy-growth-os/main/index.html`）
4. 第0.5章の「Motoさん待ち」を確認する（**まず check SQL の③**）
5. 第5章①（v849〜v852 の実機確認）へ

**Motoさんの進め方**：「憶測で修正しない」。原因を確かめ、設計 → 確認 → 実装。
分からないことは推測で埋めず聞く。**実画面を見て的確に指摘するので、作り込む前に見てもらうこと。**

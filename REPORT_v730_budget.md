# v730 — v729の動作確認と、過去月を守る追加修正

## 1. スクリーンショットから確認できたこと（v729は意図どおり動作）

| 確認項目 | 結果 |
|---|---|
| 全店の 2026-08 | **下書き** → 8月の予算を保存・提出できる状態 |
| 7月の承認 | 月別に保持（Waikiki FSP・Kaimuki が `2026-07=承認済み`） |
| ToriTon / Marujuu | `2026-07=下書き` として正しく分離 |

「承認済みが8月にも適用されて編集できない」状態は解消しました。

## 2. 追加で見つかった問題（v730で修正）

### 月データを持たない過去月が、別の月の保存で書き換わる

Waikiki Garlic Shack / Aiea / Piikoi は「月データ: なし」でした。
この3店の**7月の予算は、店舗共通の曜日設定（`b.dow`）から表示されています**。

一方、予算エディタの保存は `b.dow = _bbDow;`（共通設定の更新）を行うため、
**8月を保存した瞬間に共通設定が上書きされ、7月の予算まで変わってしまう**状態でした。

7月は承認済みなので、承認した数字が後から変わることになります。

### 対応
共通設定を上書きする**直前に、月データを持っていない過去月を「その月の月データ」として固定**します。

- 追加のみ。すでに月データがある月には触れません
- そのとき保存されていた状態（承認済み・承認者・承認日時）も一緒にその月へ移します
- 予算エディタの保存と、曜日エディタの保存の両方に入れました

### 診断画面の表示も改善
月データが無い場合でも、旧ステータスが紐づく月を表示するようにしました。
月データを持たない月には `(月データなし)` と黄色で付記されます。

例：`月別: 2026-07=承認済み(月データなし) ／ 2026-08=下書き`

## 3. 変更ファイル
- `index.html` : APP_VERSION 729 → **730**
- `sw.js` : SW_BUILD 729 → **730**

DB変更なし。既存データの削除・上書きもありません。

## 4. テスト
`verify_budget_v730.js`：実ファイルから関数を抽出して実行 — **44/44 PASS**

v730で追加した検証：
- Aiea 型（7月は承認済みだが月データなし）で、8月保存前に7月が月データとして固定される
- 固定された7月の曜日設定・承認状態・承認者が正しく移る
- その後8月の内容で共通設定を上書きしても、**7月は元の数字のまま**（修正前は変わっていた）
- すでに月データがある店（Kaimuki 型）では固定処理が走らず、7月の内容は一切変わらない
- 2回目以降の保存でも既存の月データを壊さない

回帰：v728診断 0件、v727 95/95、v719/720/721 = 21/26/33 すべてPASS。
全体構文チェック・改行（lone LF=0）確認済み。

## 5. 確認していただきたいこと

1. GM端末を v730 に更新（更新バナー → SW_BUILD 730）
2. 「保存状況の確認」を開くと、Aiea / Piikoi / Waikiki Garlic Shack にも
   `2026-07=承認済み(月データなし)` の行が出ることを確認
3. **この3店で8月の予算を入力して保存** → 保存後に7月へ切り替え、
   **7月の数字が変わっていない**ことを確認（ここが今回の要点です）
4. 8月の提出 → GM側に「GM承認待ち」が届くことを確認

## 6. これまでの修正のまとめ

| 版 | 内容 |
|---|---|
| v728 | 予算レコードの重複解消／保存・提出の結果を必ず確認／重複修復／保存状況の診断画面 |
| v729 | **提出・承認・差し戻しを月別に**（7月の承認で8月が編集不可になる問題を解消） |
| v730 | **月データを持たない過去月の保護**（別の月の保存で過去が書き換わる問題を解消） |

## 7. 残作業
- v721からの持ち越し（従業員ディスカウントカード / Tip入力権限）は未着手

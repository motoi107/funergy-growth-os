# 業務Botの案件終了（1015）

## 2026-09-11 LINE完了報告の変更（従来条件に優先）

有効な業務LINEグループでは、回答者の登録不要で案件番号＋完了報告を受け付ける。単独・複数（最大20件、カンマ・読点・空白区切り、全角可）に対応。例: `案件#1,#2,#3 確認済み` / `#1 完了` / `#1,#2 done`。完了・確認済み・修正済み・変更なし確認・その他終了の明示報告は理由省略可。勤怠・決済Void・未決済を直接doneにするため、このLINE経路では本部承認・Toast正常化を待たない。画面操作、対応継続、明示的な本部承認/差し戻しは従来条件を維持。

Webhook署名、有効グループ、そのグループの店舗範囲、LINE送信者IDを検証。匿名の公開RPCや発注操作への権限を追加しない。送信者ID、元文、日時、対象案件、結果を履歴に保存し、終了種別はline_reported（Toast検証済みとは表示しない）。対象外番号は個別に案内し、既に完了した案件・同一Webhook再配信で二重更新しない。受付結果を即時返信し、完了の定時レポートは毎朝9時HSTのまま。過去に無視されたメッセージを遡及実行しない。同一検知は閉じたまま、検知内容が変わった場合は従来通り再開する。

適用順: `20260911205005_bot_simple_completion.sql` → ops-bot。今回のLINE変更は画面版1019を変更しない。下記は旧フローおよび画面・本部操作についての仕様。


既定では勤怠の修正はToastの正常化確認後に終了する。Void・未決済、変更なし、その他終了は本部確認待ちとし、本部承認で終了、差し戻しで店舗対応へ戻す。GM/CEOは画面の「本部確認の条件」で種類・回答・金額条件を変更できる。複数条件はOR。本部確認待ちになった案件は設定変更や機械照合だけで終了させない。

番号は全店通しの #1 形式。既存案件にも作成順で番号を付け、旧B-番号は返信用別名として保持する。UUIDとsource_keyは変更しない。番号は再利用せず、同じ検知では番号を消費しない。

LINEの明示コマンド（番号と理由が必要）:
- `#123 修正済み 退勤時刻を修正` / `#123 fixed corrected clock-out`
- `#123 確認済み・変更なし 正当な取消` / `#123 confirmed no change valid cancellation`
- `#123 その他・終了 重複案件のため` / `#123 other close duplicate case`
- `#123 対応中 YYYY-MM-DD 対応内容` / `#123 in progress YYYY-MM-DD action`
- 本部: `#123 承認して終了 確認内容` / `#123 approve reason`
- 本部: `#123 差し戻し 追加確認内容` / `#123 return reason`

通知の選択ボタンは入力欄に番号と回答種別を入れる。理由を追記して送信する。自由文、質問、「了解」「完了」のみでは終了しない。古い発注コマンドは維持し、異常案件終了から発注承認を迂回できない。

## 本人・権限登録

メンション登録から回答権限を自動付与しない。本人が有効グループで「担当者登録 名前」と発言し、本部がLINEプロフィールと担当店舗を照合して「LINE回答権限の登録」で登録する。店舗管理者には担当店舗だけ、本部承認者には当該グループの店舗範囲を許可する。本部承認者の登録はGM/CEOのみ。既存のbot_users（事務Crew相当）は完了・本部承認を行えない。未登録のLINE返信は履歴のみ。実在人物の新規権限登録はこの移行には含めない。

## 通知と再検知

朝レポートの勤怠は検知時の各勤務について、開始/終了をHSTの日付付きで表示し、打刻間隔（休憩控除前）と既存設定に基づく理由を示す。退勤未打刻、時刻不正、IN/OUT逆転、重複、日跨ぎを区別。対象者・担当者・進捗・案件番号は同じブロックに保持。最大4勤務を対象優先で表示、残りは画面確認。詳細の取得失敗は明示する。保存された検知時の打刻であり、修正後の最新打刻や給与用の実働時間とは区別する。

本部確認待ち・修正反映待ち・完了には店舗向けの新規催促を送らない。対応継続の期限前は毎朝の催促候補から外す。修正反映が24時間後も確認できず実際の異常が残る場合は対応継続へ戻す。取得エラーを修正失敗と断定しない。

状態変更はトランザクション内で履歴と通知予約を保存。本部確認は既存の朝レポート用本部グループへ、差し戻しは回答元グループ（なければ本部）へ通知する。既存workerの次回実行時に最大5件を配信し、同じLINE retry keyで23時間以内のみ再試行。状態が変わった古い通知は破棄。通知失敗は案件履歴のlifecycle_noticeに残る。

2026-09-11変更: 正常化確認・本部承認などによる完了の自動通知は、ハワイ時間毎朝9:00の本部レポートへ集約する。案件の完了記録は確認時点で保存し、店舗催促もその時点で止める。完了の個別pushは送信せず、既存のpending/unknown完了通知も再送せずmorning_summary状態（daily_hq_reportへ集約、送信成功を意味しない）として履歴を保持。LINE操作への受付返信と、本部確認・差し戻しの通知は従来通り。朝レポートには前月以前の未完了、状態、理由、期限、前回レポート以降の完了を含む。9:00送信後に完了した案件は次回へ。既存の9:05/9:10再試行と日次重複防止を維持。完了詳細は最大20件表示し、続きはFunergy＋で確認する。明示的な手動再送は維持。

確認済み・変更不要の同一データは再開しない。検知内容の変更時は回答・終了状態を無効にし、履歴を保持して再確認へ戻す。旧Voidの初回fingerprint追加だけでは終了案件を再開しない。

## 適用・検証

既存mainからの変更。適用順は `supabase/migrations/20260910184520_bot_case_closure.sql` → ops-bot → index.html / sw.js 1015。既存案件を終了する移行や実在人物の権限自動付与は含まない。すべての新規RPC/sequenceはservice_role専用。実装テストは合成データのPGlite、署名付きWebhookのモック、既存Nodeテスト、静的リリースチェック。実LINEへのテスト送信は行わない。公開結果と独立レビュー対象SHAはPRと共有記録に残す。

# Funergy Growth OS 引き継ぎ（v781 → v782）

## 現在の状態

- `index.html` APP_VERSION = **782** / `sw.js` SW_BUILD = **782**（一致）
- CRLF 50,545 ・lone LF = 0 ・構文チェック OK
- 検証：`v764` `v767` `v769`〜`v782` すべてPASS（合計 498項目）
- 監査：MISS は `spl_skills_st_` の1件のみ／要確認3・解決済み7（**v772から変化なし**）

## ⚠️ デプロイ状況

| 版 | 内容 | 状態 |
|---|---|---|
| 〜v772 | — | 本番反映済み |
| v773 | 容量警告・自動pruneの撤去 | **未デプロイ** |
| v774 | 本部目標の設定タブ | **未デプロイ** |
| v775 | chef ロールの本部所属化 | **未デプロイ** |
| v776 | Office ログインの反映と表記 | **未デプロイ** |
| v777 | 役職の選択肢に Chef を追加 | **未デプロイ** |
| v778 | 役職ラベルを Head Chef へ（G4と一致） | **未デプロイ** |
| v779 | Office ログインの並びを Grade 順へ | **未デプロイ** |
| v780 | Kaimuki/Piikoi スキル基準 ver2 | **未デプロイ** |
| v781 | Miso の日英を一致（英語が正） | **未デプロイ** |
| v782 | 行動計画に「品質管理」グループを追加 | **未デプロイ** |

**v773 は保存層に関わるので、まず一周してから残りの確認へ進むこと。**

---

## このセッションでやったこと（v777）

### 役職の選択肢に Corporate Chef が無かった

**根本原因**：従業員フォームの役職ドロップダウン（28773行）が

```
['ceo','gm','office','am','g4','sl','g2','office_crew','crew']
```

で、**`chef` が入っていなかった**。v766 で chef ロールを作ったとき、
ログイン候補・メニュー・グレードには追加したが、**役職を選ぶ画面だけ漏れていた**。

そのため UI から Corporate Chef を設定できず、キッチン管理タブに到達する手段が無かった。
配属店舗を Office にしても、それは所属を変えるだけで役職は変わらないので解決しない。

**修正**：`'office'` の次に `'chef'` を追加（本部側でまとめる）。1行。

### 追加に必要なものは全部揃っていた

| 参照先 | 状態 |
|---|---|
| `ROLE_CONFIG.chef` | v766 で定義済み（label `Corporate Chef`・`financial:true`・`allStores:true`） |
| `PERM_LEVELS` | `manager` あり。`permFromRole('chef')='manager'`（v775で追加） |
| `ROLE_ALIAS` | `chef` は無い → ログイン後も `curRole` は `chef` のまま |
| `empRoleChanged()` | `permFromRole()` を使うので自動で追従 |

つまり**選択肢に1つ足すだけで通る**状態だった。

---

### 役職ラベルが等級と食い違っていた（v778）

役職 `chef` は **グレード G4** なのに、ラベルが **`Corporate Chef`** だった。
`GRADE_TITLES` では **Corporate Chef=G5 / Head Chef=G4** なので、役職名と等級が食い違う。

**修正**：`ROLE_CONFIG.chef` の `label` / `name` を **`Head Chef`** へ。
役職の中身（`financial` / `allStores` / `canApprove` / `canCreate` / perms / メニュー）は据え置き。

**G5 へ上げるとき**は `title` を `Corporate Chef` にすればよい。
`empGrade()` は title 優先なので G5 を返す（役職は `chef` のままでよい）。

---

### キッチン管理の行動計画に「品質管理」を追加（v782）

`CC_GROUPS` に **品質管理(g_qc)** を新設（衛生管理の次）。指標6件を追加した。

| id | 指標名 | 単位 | 良い方向 |
|---|---|---|---|
| `m_ops` | 提供時間 | 秒 | 低い方が良い |
| `m_loss` | ロス率 | % | 低い方が良い |
| `m_recipe` | レシピ登録率（全商品） | % | 高い方が良い |
| `m_recupd` | レシピ未更新 | 件 | 低い方が良い |
| `m_std` | 提供の標準化レベル | Lv | 高い方が良い |
| `m_qclv` | 品質管理レベル | Lv | 高い方が良い |

**単位・良い方向・グループ・対象店舗は、指標編集（本部のみ）から変更できる。**
測り方の指定が無かったので上記を既定にした。合わなければ画面で直せる。

**既定は全て任意（`req` なし）。** v774 の方針どおり、運用が回ってから設定タブで必須に切り替える。
既定目標も未設定なので、設定タブでGMが入れる。

#### quality カテゴリーをメニュー開発から移した

`CC_CATS` には以前から `quality`「クオリティ管理」があったが、
`CC_CAT_GROUP` で **`g_dev`（メニュー開発）に同居**していた。これを `g_qc` へ付け替えた。

**既存データへの影響**：`cat='quality'` で登録済みの行動は、
メニュー開発の欄から品質管理の欄へ**表示が移る**。行動そのものは消えない。

`CC_GROUP_CAT.g_qc = 'quality'` も足したので、品質管理の店舗カードから行動を足すと
カテゴリーが自動で「クオリティ管理」になる。

#### verify_v769 の3件は設計変更で更新

「グループが5つ」「メニュー開発に2件（商品開発＋品質）」「網羅チェックの集計」が
v769 当時の構成を前提にしていた。**製品の退行ではない**ので、6グループ・
quality は品質管理・集計に `g_qc` を含める、へ更新した。

---

### Kaimuki / Piikoi のスキル基準を ver2 に更新（v780）

Motoさん提供の ver2 と全35項目を突き合わせ、差分のみ反映した。

| 軸 | 変更 |
|---|---|
| Team Standards | 変更なし |
| Main Plater | 変更なし |
| **Miso** | Lv1 EN に `seaweed` 追加／**Lv2・Lv3・Lv4 を全面差し替え** |
| **Host** | Lv3 EN を `(menu, recommendations, guidance, checks)` の4項目列挙へ |
| **Supporter** | **Lv1〜Lv4 を全面差し替え**（Lv3 は「味噌」→「味噌汁」） |
| Prep | 変更なし |
| Hospitality | 変更なし |

7軸 × Lv0〜4 = **35項目・満点28点**。チップ率条件（14点 / 23点）は据え置き。

#### 原本の日英食い違いは「英語が正」で決着（v781）

Motoさん確認により、Miso Lv1・Lv2 とも**英語を正**として日本語を合わせた。

| | 修正前（原本） | 修正後 |
|---|---|---|
| Lv1 配置 | 日：丼/味噌/スプーン（3項目）<br>英：seaweed/bowl/miso/spoon（4項目） | 日：**海苔**/丼/味噌/スプーン（4項目） |
| Lv2 オーダー数 | 日：**70**オーダー＋洗い場<br>英：**50+** orders・clean station | 日：**エリアの清潔を維持しながら、50オーダー以上を遅れなく安定して対応できる。** |

Lv2 は数字だけでなく内容も合わせた。原本の日本語「洗い場を回す」は **Lv3 の要件と重複**していたため
（Lv3＝70オーダー＋洗い場・フロアサポート）、英語どおり「エリアの清潔維持」に直した。
これで Miso も Host と同じ **50 → 70** の刻みになる。

`verify_v781` に、**全軸で日英のオーダー数が一致すること**と
**Lv2 < Lv3 の単調性**を機械チェックする項目を入れた。

#### 原本の誤字は採用しなかった

- `Seeweed` → **`seaweed`** に修正して反映
- Host Lv4 の `cusomer` → 既存の **`customer`** のまま

---

### Office ログインの並びを Grade 順にした（v779）

指定：**CEO → GM → AM → Office Manager(経理) → Head Chef → 事務Crew**

従来は `{ceo:0,gm:1,office:2,chef:3,office_crew:4,am:5,...}` で、
**経理(G4) と Head Chef(G4) が AM(G5) より前**に出ていた。

`empGradeNum()` の降順では並べられない。**`empGrade()` の役職→等級表に `ceo` と `office` が無い**ため、
title 未設定だと 0 になって先頭に来られない。そこで指定の順序を明示的に持たせた。

```
var order={ceo:0,gm:1,am:2,office:3,chef:4,office_crew:5};
```

ここに無い役職（配属だけ OFFICE の店舗スタッフ）は、そのあとに**実Grade降順**で並ぶ。
店舗側の並び（実Grade降順）は変えていない。

**表示ラベルは変えていない。** 現状は `am`→`G5(エリアMG)`、`office`→`経理`。
指定の呼称（AM / Office Manager）と食い違うので、揃えるなら次回。
`chef` は `roleLabelI18n` に無く `ROLE_CONFIG.chef.label` に落ちるので **Head Chef** と出る。

---

## Chikayoshi Nishihara の設定（v779 デプロイ後）

| 項目 | 値 |
|---|---|
| `role` | **Head Chef**（選択肢に出るようになる。グレード G4） |
| `title` | **Head Chef** |
| 配属店舗 | 全店(ALL) でも Office でも可 |

`role` を Head Chef にすれば、配属店舗に関係なくログイン候補に出る（v766）。
配属を Office にする必要は無くなる。

**`title` の注意**：`GRADE_TITLES` では **`Head Chef`=G4 / `Corporate Chef`=G5**。
8/1のG4昇格なら `Head Chef`。`title` が空でも role から G4 になる。

### ログイン後に見えるもの

`CHEF_GROUPS`（キッチン管理）の4グループ・15ページ。

- **本部** … 月間行動計画 / 通知 / 承認
- **食材管理** … メニュー / レシピ / 食材差異 / 食材マスター / 食材移動 / 仕入先 / 棚卸
- **原価** … フードコスト
- **マイページ** … マイページ / ハンドブック / プロフィール / 設定

15ページすべて `OFFICE_OK_PAGES` にあることを `verify_v777` テスト5で機械チェック済み。
`chef` は店舗切替で Office を選べないので `curStore` が `OFFICE` にならず、
ページゲートにも掛からない。

---

## Motoさんの手作業

| # | 内容 |
|---|---|
| 1 | **Chikayoshi Nishihara の役職を Head Chef、title を Head Chef に変更＋PIN設定**（v777/v778） |
| 2 | **Kaimuki/Piikoi のスキル基準の移行を「レベルを引き継ぐ」で再実行**（GM・CEOのみ。これをしないと ver2 の文言が端末・クラウドに反映されない） |
| 3 | **品質管理6指標の単位・目標を確認**（v782。測り方の指定が無かったため既定を入れてある。指標編集で変更可） |
| 4 | **設定タブで各店の原価目標％と G3・G2 の人数目標を入力**（v774。既定値なし） |
| 5 | **旧指標 `m_dev` を画面から削除**（`m_grand`/`m_seasonal` と重複） |
| 6 | **DOHチェック項目27件を実際の検査票と突き合わせ**（Chikaさんと） |
| 7 | **Marujuu 7月の予算を入れ直す**／6月に見覚えのない数字が無いか確認 |
| 8 | **他店舗の当月予算が意図した数字か確認** |
| 9 | **Kaimuki / Piikoi のスキルレベルと Career Score を一度保存し直す** |
| 10 | v752 のスキル修正について**スタッフからの確認結果**を待ち |

---

## 次回の優先タスク

1. **v773〜v782 をデプロイして実機で一周**
2. **Chef での実ログイン確認** — キッチン管理15ページ・勤怠打刻・給与対象・グレードG4表示
3. **ログイン画面の役職ラベルを揃えるか** — `am`→`AM`、`office`→`Office Manager`（v779で並びのみ変更）
4. **メニュー内の「本部」表記を Office に揃えるか**（v776 でログイン画面のみ変更済み）
4. **hq_goals（フリーテキストの本部目標）の整理** — 設定タブが本体になったので両方出ている
5. **指標ベースのカバー率** — 現在は `hq_goals` への紐づけを見ている。4と一緒に判断
6. **二重書きの停止**（IDB移行の最終段階）
7. **既存データとの連携** — 原価率を予実から、人員をシフトから、育成をスキルから自動取得
8. **商品開発フロー** — 提案→試作→試食→AM以上の最終承認→販売準備→販売後評価
9. 持ち越し：①従業員ディスカウント証明カード ②Tip入力権限の調査
10. HANDOFF_v747 から未着手：Qサマリー追加項目、「翌月」問題の全画面点検、日次食材差異の初期設定

---

## 検証・監査スクリプト

| ファイル | 用途 |
|---|---|
| `verify_v762`〜`v768` | 前版の `index_v76x_backup.html` が要る |
| `verify_v769`〜`v772` | グループ化・差の計算・承認後の固定・週次更新 |
| `verify_v773` | 容量警告・自動pruneの撤去 |
| `verify_v774` | 設定タブと目標の反映 |
| `verify_v775` | chef ロールの欠落4件 |
| `verify_v776` | Officeログインの候補と表記・curStore の決まり方 |
| `verify_v777` | 役職の選択肢。ドロップダウンを実際に組み立てて確認 |
| `verify_v778` | 役職ラベルと等級の一致 |
| `verify_v779` | Officeログインの並び |
| `verify_v780` | スキル基準 ver2。全35項目を評価して突き合わせ |
| `verify_v781` | Miso の日英一致。オーダー数の整合と単調性 |
| **`verify_v782.js`** | **品質管理グループの追加。7観点35項目** |
| `audit_merge_coverage.js` / `audit_budget_month.js` | 合流ルール／予算の対象月 |

**次回のzipには `index_v782_backup.html` を同梱すること。**

---

## このセッションで学んだこと

1. **機能を足したら「設定する画面」も足す。** v766 は chef を
   ログイン候補・メニュー・`empGrade()` に足し、v775 で perms・グレード・名簿を直したが、
   **役職を選ぶドロップダウンだけ最後まで漏れていた**。
   出口（使う側）ばかり直して、入口（設定する側）を見ていなかった。
   **新しい選択肢を作ったら、それを人がどこで選ぶのかを必ず確認する**
2. **ユーザーの回避策は、原因の手掛かりになる。** 「配属をオフィスにしないとログインできない」は、
   役職を変えられないので配属で代用しようとしていた、ということだった。
   回避策そのものを直すのではなく、なぜ回避が必要だったかを見る
3. **原本の中の食い違いは、直さずに指摘する。** ver2 の Miso Lv2 は日本語 70／英語 50 で
   食い違っていた。他ポジションの刻みから英語が正らしいと推測はできるが、**基準表は運用の根拠**なので
   こちらで寄せずに原本どおり入れ、判断を仰いだ（v781 で「英語が正」と確定）。
   明らかな綴り誤り（Seeweed）だけは直した
4. **既にある部品を使えないか先に見る。** 品質管理を足すとき、`CC_CATS` には既に
   `quality`「クオリティ管理」があり、グループだけが無かった。
   カテゴリーを増やさず、グループへ付け替えるだけで済んだ
5. **食い違いを直したら、同じ種類の食い違いを検出する仕組みを残す。** v781 では
   「全軸で日英のオーダー数が一致するか」を機械チェックにした。
   次に基準表を更新したとき、同じ取りこぼしが自動で見つかる
6. **文字列がエスケープのまま入っていることがある。** `store\u2019s` は実体の U+2019 ではなく
   `\u2019` という6文字がソースに入っていた。`assert count==1` で止まって気付けた。
   **アンカーは目で見た表示ではなく、バイト列で確認する**
7. **抽出範囲は「開始位置より後ろ」を探す。** `verify_v779` で並べ替えを切り出すとき、
   終端の目印 `ng.innerHTML` が開始位置より**前**にも出ていて、範囲が逆転して空文字になった。
   テストは通らず全件落ちたので気付けたが、**抽出結果が空でないことを明示的に確認する**ようにした
8. **役職名と等級は別の語彙。混ぜると必ずどこかで食い違う。** 役職 `chef` は
   「どのメニューを出すか」を決めるもので、等級は `title` が決める。
   そこに G5 の呼称（Corporate Chef）を役職ラベルに使ったため、G4 の役職に G5 の名前が付いた。
   **役職は役割で名づけ、等級は title に持たせる**
9. **ハーネスの正規表現も検査対象。** 役職一覧を `[a-z_]+` で拾おうとして `g4`/`g2` を取りこぼし、
   テストが空配列で落ちた。**製品ではなくハーネスの不具合**（今回も切り分けてから直した）

---

## ビルド手順（厳守）

1. Python で透過的置換。`open(newline='')` で CRLF 保持、アンカーは `assert count==1`
2. Node `new Function()` で構文チェック（`MINUTES_SEED`/`HANDBOOK_SEED` をフィルタしない）
3. `APP_VERSION` と `SW_BUILD` を**同時に**上げる
4. lone LF = 0 を確認
5. `/mnt/user-data/outputs/` へコピーして `present_files`
6. 危険な処理は実関数を切り出して Node でモック実行し、**修正前の症状を再現するテストを必ず入れる**
7. 大きな新規ブロックは別ファイル（LF）で作り、CRLFに変換してから差し込む

**Motoさんの開発方針：「憶測で修正しない」。根本原因を確認 → 設計 → 確認 → 実装を1ステップずつ。**

---

## 主要な定数・環境

- ロール/グレード：crew=G1, g2=G2, sl=G3, g4=G4, **chef=G4**, am=G5, gm=G6
  - `GRADE_TITLES`：**Head Chef=G4 / Corporate Chef=G5**
  - `empGrade()` は title 優先→role フォールバック。`myGradeKey()` は `Math.max(title, role)`
  - `ROLE_ALIAS = {g2:'sl', g4:'am'}`。**chef は含まれない**
  - 役職の選択肢：`['ceo','gm','office','chef','am','g4','sl','g2','office_crew','crew']`（v777）
  - 役職 `chef` の表示名は **Head Chef**（v778）。G5 にするなら title を `Corporate Chef` に
  - Office ログインの並び：`{ceo:0,gm:1,am:2,office:3,chef:4,office_crew:5}`、以降は実Grade降順（v779）
- ログイン候補：Office 側は **役職 ∪ 配属店舗=OFFICE**（v776）／店舗側は `store` と `support`
- Office ログインは `curStore='ALL'`。`curStore='OFFICE'` は店舗切替のみ（ceo/gm/office/office_crew）
- ページゲート：`curStore==='OFFICE'` のとき `OFFICE_OK_PAGES` 以外は Office管理へ差し替え
- 本部のメニュー：CEO/GM = `OFFICE_GROUPS`／経理 = `ACCT_GROUPS`／Chef = `CHEF_GROUPS`
- 本部の名簿：経理側 = `officeMembers()`／本部スタッフ全般 = `hqStaffMembers()`（v775）
- 店舗：ToriTon(F01) / Tenkichi(F02) / FSP(F03) / Waikiki Garlic Shack(F03-G) /
  Totoya Kaimuki(F04-K)・Piikoi(F04-P)・Aiea(F04-A) / Marujuu(F05)
- Corporate Chef の目標：`ccScoreView()` が唯一の読み出し口
- 保存層：`ls` / `lsSet` / `lsKeys` / `_lsStoreGet` / `_lsRawStr` / `_lsRawDel` / `_lsWriteVerified`
  - **新しい保存経路を作らない**／自動pruneは `_lsPruneNeeded()` でしか走らない（v773）
- Supabase: `tgbhgxzehzeouopklhje` / Edge Function: `hyper-worker`, `auth-pin`, `emp-private`, `ob-private`
- GitHub: `motoi107/funergy-growth-os` → `funergy-plus.com`

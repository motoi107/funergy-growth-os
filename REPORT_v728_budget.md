# v728 — 予算が保存されない／GMに届かない：調査結果と対応

## A. 調査でわかったこと（コードで確認した事実）

### ① 同じ店舗の予算が「2件」になる状態がある【実コードで再現】

予算は `spl_budgets_v2_<店>`（分割キー）に保存されますが、端末には旧い統合キー
`budgets_v2` も残ります。v720 の畳み込み処理は「レコードに `id` が無いキーは対象外」
という安全弁があり、**予算はここに該当するため統合キーが消えません**。

読み出し `_splitRead()` は統合キーと分割キーを和集合にしますが、`id` が無いレコードは
**JSON が完全一致しないと同一と判定できません**。中身が少しでも違えば別レコード扱いになり、
`getBudgets()` が同じ店舗を2件返していました。

実コードを抽出して再現した結果（修正前）：

| 確認項目 | 結果 |
|---|---|
| `getBudgets()` の戻り | F04-K が **2件**（入力済み＋空の殻） |
| 保存後の分割キー | **2件**が書き戻される |
| `_coversBudgets`（保存できたかの検証） | **常に false** ＝ 検証が通らない |

`_coversBudgets` は店舗IDでマップを作るため、**重複があると後ろの1件（空の殻）で上書き**され、
月データを持たない方が判定に使われて必ず失敗していました。検証が通らないと
`opMergePush` は3回リトライして諦め、未送信キューに残り続けます。

### ② 保存に失敗しても「保存しました」と表示していた【コードから確定】

- `saveBudgets()` は `lsSet()` の戻り値を**捨てていました**
- `bbSave()` は結果を確認せず、無条件に「予算を保存しました（月間 $…）」と表示
- `_doSubmitBudget()` も同様に、保存結果を見ずに「GMへ承認申請を送信しました」と表示

端末の空き容量不足などで保存できなかった場合、**画面上は成功したように見えるのに
何も保存されていない**状態になります。「提出まで押したのに届いていない」は、
この経路で起きた可能性が高いと考えます。

### ③ 「合計金額だけある」について

月データ（`b.months[<年月>]`）はマージ時に必ず両方の和集合になる一方、曜日設定などの
明細は「勝った方のレコード」からしか引き継がれません。重複や空の殻が絡むと
**月の合計だけ残って内訳が消える**形になり得ます。実機の状態は下記の診断画面で確認できます。

### ④ 現時点で断定できていないこと

上記①②は確実な欠陥ですが、**今回の事象がどちらで起きたかは端末の状態を見ないと確定できません**。
そのため、原因を特定できる診断画面を追加しました（下記 C）。

## B. 実施した修正

| # | 修正 | 内容 |
|---|---|---|
| 1 | `_splitRead` の重複解消 | `id` を持たないレコードは、そのキーのマージ定義（`mergeBudgets`）で正しく1件に畳む。内容の濃い方・新しい方が残り、月データは和集合。 |
| 2 | `saveBudgets` が結果を返す | 保存できたか呼び出し元へ返すようにした。 |
| 3 | `bbSave` の保存確認 | 保存後に**読み直して確認**。失敗時は「保存できませんでした」と赤で表示し、**未保存フラグを立てたまま**にする（画面の入力は消えない）。 |
| 4 | 提出の確認 | 提出時も読み直して `GM承認待ち` が保存されたか確認。保存できていなければ提出扱いにしない。保存後は**未送信キューを送信し、残っていれば警告**。 |
| 5 | 重複の修復 | 既に書き込まれてしまった重複を1件に畳む。起動時に一度だけ自動実行＋診断画面から手動実行も可能。**削除はせず内容は保持**。 |
| 6 | 保存状況の診断画面 | 予算設定画面に「保存状況の確認」ボタンを追加。 |

## C. 「保存状況の確認」でわかること

予実 ＞ 予算設定 の右上のボタンから、**店舗の端末でもGMの端末でも**実行できます。

- 店舗ごとの：ステータス／この端末の保存件数／旧キーの件数／**重複の有無**／
  保存されている月の一覧／今月のデータ有無／提出日時と提出者
- **未送信（クラウド未反映）の件数** ＋「今すぐ送信」ボタン
- **クラウド側**の各店のステータスと月データ（重複があれば赤字で表示）
- 「重複を修復」ボタン

## D. 確認していただきたい手順

1. GM端末を v728 に更新（更新バナー → SW_BUILD 728）
2. 予実 ＞ 予算設定 ＞「保存状況の確認」を開き、**クラウド**の欄に該当店舗の予算があるか確認
   - **クラウドに無い** → 店舗端末から送信できていない（②が原因）
   - **クラウドにあるがステータスが下書き** → 提出が保存できていない
   - **重複あり（赤字）** → 「重複を修復」を実行
3. 店舗端末も v728 に更新し、同じ画面を開いて
   - 「未送信」が0でなければ「今すぐ送信」
   - 「重複」が出ていれば「重複を修復」
4. その状態で予算を入力し直して保存 → **失敗すれば必ず赤いメッセージが出ます**（今後は黙って消えません）

万一データが消えている場合、この端末には保存前スナップショットが3世代残っています
（`budgets_v2_bak1〜3`）。設定センターの復元機能から、**今ある予算を壊さずに**
空になっている店舗だけ穴埋め復元できます。

## E. 変更ファイル
- `index.html` : APP_VERSION 727 → **728**
- `sw.js` : SW_BUILD 727 → **728**

DBの変更はありません。既存データの削除・上書きもしていません。

## F. テスト
- `verify_budget_v728.js`：実ファイルから関数を抽出して実行 — **13項目すべてOK（修正前は4件が問題）**
  - 重複が返らない／保存で増えない／`_coversBudgets` が通る／マージで内容が消えない
  - 修復関数が重複を1件に畳み、**dow・ステータス・月データが残る**
  - `verifyBudgetSaved` が未保存を正しく検出する
- 既存の回帰：`verify_v727.js` 95/95、`verify_v719/720/721` 21/26/33 すべてPASS
- 全体構文チェック・改行（lone LF=0）確認済み

# v735 — Invoice の「食材 / 食材以外」を読み取り時に自動判定

## 1. ご質問への回答：はい、原価に入っています

**根本原因は「Invoice に用途の入力欄そのものが無かった」ことです。**

用途欄があるのは **Receipt（買い出し）と Reimburse（立替）だけ**で、
Invoice（業者請求書）には最初から存在しませんでした。そのため：

```
Invoice を保存 → purpose = ''（空のまま）
                 ↓
function _fcInCost(purpose){
  if(!p) return true;    // 未設定＝原価に含める
}
                 ↓
製氷機の修繕も、必ず Food Cost に入る
```

さらに、Invoice 用の用途一覧には「**修繕・設備**」がありませんでした
（買い出し側にはあります）。仮に欄があっても正しく分類できない状態でした。

仕入明細の仕入額も `Reimburse` を除くだけで、用途で絞っていませんでした。

## 2. 自動判定できます（実装しました）

読み取り時に、次の順で判定します。**上ほど確かな根拠**で、推測は1つの関数に閉じ込めています。

| 順 | 根拠 | 確度 |
|---|---|---|
| ① | **業者マスタの区分**（一度答えたら以後は自動） | 高 |
| ② | **同じ業者の過去の用途**（2件以上の多数決） | 高 |
| ③ | **明細の食材一致率** — OCRが食材マスタに紐付けた行の金額比率 | 中 |
| ④ | 業者名・明細名の**キーワード** | 中 |

③が効くのは、OCR がすでに明細を食材マスタと照合して `code` を付けているためです。
**60%以上が食材 → 食材／10%以下＋非食材の語 → 食材以外**。

どれでも決まらなければ**勝手に決めず**、その場で選んでもらいます。

### 製氷機の修繕の例
```
明細: ICE MACHINE REPAIR - SERVICE CALL / LABOR 2HR / PARTS - WATER FILTER
→ 食材一致率 0%、「ice machine」を検出
→ 自動判定：食材以外（修繕・設備）→ 原価から外れる
```

## 3. 画面の動き

1. Invoice を読み取ると、**「食材 / 食材以外」**の選択が出ます（必須）
2. 自動判定の結果があらかじめ選ばれ、**根拠が表示されます**
   （例：`自動判定: 食材以外（修繕・設備） — 「ice machine」を検出`）
3. 違えばワンタップで変更。食材以外なら用途（修繕・設備／備品・消耗品／その他）を選択
4. 「この業者の区分として記憶する」にチェックが入っており、**次回から自動**になります
5. 選ばずに保存しようとすると止まります（**未設定のまま原価に入ることはもうありません**）

## 4. すでに登録済みの分（ご要望どおり一覧でまとめて）

Invoice管理の画面に「**未分類 ○件**」ボタンが出ます。押すと一覧で分類できます。

- 各件に**自動判定の候補があらかじめ選択済み**（根拠つき）
- 「すべて食材」「すべて食材以外」の一括切替
- 業者の区分としても同時に記憶
- 保存すると**その分が原価から外れます**

## 5. 変更ファイル
- `index.html` : APP_VERSION 734 → **735**
- `sw.js` : SW_BUILD 734 → **735**

DB変更なし。既存データの書き換えもしません（分類は画面から明示的に実行したときだけ）。

## 6. テスト
`verify_v735.js` — **40/40 PASS**

- 用途未設定が原価に入る従来動作の確認／修繕・備品・その他は原価外
- 明細の食材一致率（97%→食材、0%→食材以外）
- **製氷機の修繕の実例で「食材以外・修繕・設備」と判定され、原価から外れる**
- 業者マスタに記憶したら次回から確度高で自動／表記ゆれ（大文字）も一致
- 過去実績の多数決（3件中2件）／**1件だけでは判定しない**（推測しすぎない）
- 判定できないときは空を返す（勝手に決めない）
- 優先順位（業者マスタ > 過去実績 > 明細 > 語句）
- 未分類の抽出（立替・削除済みは除く）
- 画面側の配線9項目

回帰：v734 31/31、v732 予算 70/70、v728診断 0件、v727 95/95、v719/720/721 = 21/26/33。

## 7. 確認していただきたいこと

1. v735 に更新（更新バナー → SW_BUILD 735）
2. Invoice管理を開き、「**未分類 ○件**」ボタンから一覧を確認
   → 製氷機の修繕が「食材以外（修繕・設備）」で候補選択されているはずです
3. 保存後、Food Cost が下がることを確認
4. 新しく Invoice を1件読み取り、自動判定が出ることを確認

## 8. 判定語句の追加について
`INV_NONFOOD_WORDS` に語句を足せば判定を強化できます。現在は repair / labor / rental /
plumb / electric / refrigerat / ice machine / linen / uniform / waste / 修繕 / 修理 / 点検 /
工事 / リース / 清掃 / 廃棄 など約40語です。よく来る業者名や明細の言い回しがあれば教えてください。

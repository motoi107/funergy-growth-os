# v729 — 原因確定：予算のステータスが「月ごと」になっていなかった

## 1. ご指摘のとおりでした。これが本件の原因です

スクリーンショットで「8月の画面なのに承認済みは7月のもの」というご指摘、そのとおりでした。
**予算のステータス（下書き／GM承認待ち／承認済み／差し戻し）は、これまで店舗に1つしか無く、
月の区別がありませんでした。**

そのため、7月を承認済みにした店舗では次のことが起きていました。

```
canEditBudget(b)  →  b.status（= 7月の「承認済み」）で判定
                  →  承認済みはGM以上のみ編集可
                  →  店長・AMは 8月の予算を保存できない
```

`bbSave()` は先頭で `canEditBudget()` を見て弾くため、**8月の予算を入力して保存を押しても
そこで止まっていました**。7月の月データは残っているので、画面には**合計金額だけが表示される**——
「予算を作成しても保存されない（なぜか合計金額だけある）」という症状と一致します。

スクリーンショットの状態がまさにこれです。

| 店舗 | 表示 | 月データ | 実際に起きていたこと |
|---|---|---|---|
| Kaimuki / Piikoi / Aiea / Waikiki FSP / WGS | 承認済み | 2026-07 か なし | **8月が編集不可 → 保存も提出もできない** |
| ToriTon / Tenkichi / Marujuu | 下書き | 2026-08 あり | 8月を保存できている |

8月の月データがあるのは「下書き」の3店だけ、という点も裏付けになっています。

## 2. 修正内容（v729）

### 提出・承認・差し戻しを月ごとに持つようにした
`b.months[<年月>]` に status / 提出者・提出日時 / 承認者・承認日時 / 差し戻し理由 を保存します。

- `budStatusFor(b, ym)` … その月の状態を返す
- `budSetStatus(b, ym, 状態, 付随情報)` … その月にだけ記録する
- `canEditBudget(b, ym)` … **月ごとに編集可否を判定**（月を省略すると画面で選んでいる月）

### 対応した画面・処理
- 予算エディタの保存 / 曜日エディタの保存 / 区分エディタの保存
- 提出（GM承認待ち）・GM承認・差し戻し・再オープン
- 予算設定の一覧のステータス表示、エディタ見出しのステータス表示
- 「GM承認待ちの予算が◯店あります」の件数（**選んでいる月**で数える）
- 保存状況の確認画面（月ごとの状態を並べて表示）

### 旧データの扱い（非破壊）
起動時に一度だけ、既存の `status` が**どの月のものか**を判定して記録します。
判定はこの順序で、推測はこの1か所に閉じ込めています。

1. 月データがあれば、その中で一番新しい月
2. 無ければ `period`（例 `2026.7` → `2026-07`）

その結果、**7月の「承認済み」は7月のまま**、**8月は「下書き」**になります。
既存データを書き換えたり削除したりはしません。

## 3. 変更ファイル
- `index.html` : APP_VERSION 728 → **729**
- `sw.js` : SW_BUILD 728 → **729**

DB変更なし。

## 4. テスト
`verify_budget_v729.js`：実ファイルから関数を抽出して実行 — **32/32 PASS**

スクリーンショットと同じ状態（7月承認済み・8月画面）を作って検証しています。

- 7月は承認済みのまま／8月は下書き／承認者は7月にだけ紐づく
- **8月をSLが編集できる**（修正前は false で保存が弾かれていた）／7月は編集不可のまま
- 8月を提出しても7月の承認が壊れない／8月承認後も両月が独立／9月はまだ下書き
- 保存して読み直しても月別状態が保たれる／店舗の重複なし／提出の保存確認が通る
- 月データが無い店（Waikiki Garlic Shack 型）も `period` から7月と判定し、8月は編集可
- 差し戻し → 再提出も月別、理由も月に紐づく
- 旧データ補完を2回走らせても既存の月別状態を壊さない

回帰：v728の予算診断 0件、v727 95/95、v719/720/721 = 21/26/33 すべてPASS。
全体構文チェック・改行（lone LF=0）確認済み。

## 5. 確認していただきたいこと

1. GM端末を v729 に更新（更新バナー → SW_BUILD 729）
2. 8月を選んだ状態で予算設定を開く → **承認済みだった5店が「下書き」になっている**ことを確認
3. 「保存状況の確認」を開くと、月別の状態（`2026-07=承認済み ／ 2026-08=下書き` のように）が並びます
4. 店舗端末も更新し、8月の予算を入力 → **保存できること**、提出できることを確認
5. 7月を選び直すと、承認済みの表示と承認者が元のまま残っていることを確認

## 6. 残作業
- v728で入れた保存失敗の検知・重複修復・保存状況の確認はそのまま有効です
- 万一データが消えている場合、保存前スナップショット（3世代）から穴埋め復元できます
- v721からの持ち越し（従業員ディスカウントカード / Tip入力権限）は未着手

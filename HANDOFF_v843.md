# Funergy Growth OS — 引き継ぎ v843

**v843：重要KPI を「達成した店舗数」から「今月の進捗と目標との差」へ差し替え。**

---

## 0. 最初にやること

```bash
unzip funergy_v843_handoff.zip
tar -xJf handoff_v843.tar.xz
cd handoff_v843
TZ=Pacific/Honolulu node run_verify.js
node make_preview.js        # → preview.html
```

**期待値**

```
合計 PASS=3745  FAIL=0  実行=67/82
合流ルールの未定義: 1        ← spl_skills_st_ の1件だけが既知
同期対象で未保護: 0
予算の対象月: 3 / 7
APP_VERSION=843 / SW_BUILD=843
CRLF=57487 / lone LF=0
```

**v836〜v843 とも未 push。**GitHub は md5 で突き合わせること。

---

## 1. v843 でやったこと

### 重要KPI の表示を差し替えた

v842 は「達成 / 未達 / 判定不可」の**店舗数**を積み上げ帯で出していた。
これでは**目標にどれだけ足りないか**が分からない、という指摘。

**実績・目標・差 を主にし、店舗数は補助に落とした。**

```
売上      $400,000        −$10,000     [====帯====]
          目標 $410,000    97.6%       達成 2店・未達 1店

人件費率  28.75%          +0.75pt      [====帯====]
          目標 28.0%       102.7%      達成 2店・未達 1店
```

積み上げ帯（`md-cbar`）は廃止。行を選ぶと店舗別の内訳が開くのは v842 のまま。

### 複数店舗のまとめ方（新しい定義は作っていない）

`MD_KPI` に **`agg`** を足した。

| KPI | agg | まとめ方 |
|---|---|---|
| 売上・客数・営業利益 | `sum` | 足す |
| 人件費率・原価率 | `rate` | **売上で重みを付けた平均** |
| 客単価 | `avg` | **客数で重みを付けた平均** |

**`Σ(売上 × 率) ÷ Σ売上` は `総額 ÷ 総売上` と同じ値になる。**
つまり売上加重の平均は「全社の本当の率」そのもので、新しい定義ではない。
既存ダッシュボードの `wFC` / `wL` と同じ式でもある。
この一致は `verify_v843` [2] で毎回確かめている。

客単価も同じ理屈で、客数加重＝`総売上 ÷ 総客数`。

### 判定できない店舗は集計に入れない

目標が入っていない店舗を混ぜると、目標側が歪む。

- 集計は**判定できた店舗だけ**で行う
- **何店ぶんの数字かを必ず画面に出す**（例：`目標 28.0%（2/3店ぶん）`）
- 判定不可の店舗数も併記する
- **全店が判定不可なら数字を出さず、理由と直し方を出す**（`0%` と表示しない）

---

## 2. 進み具合

| 版 | 内容 | 状態 |
|---|---|---|
| v837〜v842 | 判定層・必須KPI設定・器・統合・フル幅・実画面反映 | 完了 |
| v843 | **重要KPI を進捗と目標との差へ** | **完了** |
| v844 | 役職別の作り込み（Head Chef の原価・仕入中心 ／ CEO の絞り込み） | 次 |
| v845 | Work Center の本統合 ＋ 管理統括の「整備中」を外す | |
| v846 | 店長通知の画面連結 | |

---

## 3. 未確認・保留

### ① まず設定を入れてほしい（ここが入らないと数字が出ない）

本番では 人件費率・原価率・営業利益・客数・客単価 が**全8店で判定不可**。原因は設定：

- **KPI管理 → 店舗タブ → 人件費率・原価率の目標**（全8店）
- **本部予算（客数×客単価・営業利益）** … `hqPlanForYm` が読む

入れた瞬間に判定不可がほどけ、v843 の「目標との差」が実データで出る。
画面には「どこを直せばよいか」と、その画面へ飛ぶボタンを出してある。

### ② 管理統括

- **`management_control_v815.sql` の流し直しが未実施**（v824 の chef 追加ぶん）
- 管理統括はまだ「整備中」表示中。v845 で外す
- **7月（前月）の検知結果を Moto さんがまだ見ていない**

### ③ その他

- 昨対（2025年データ）… 枠だけ。入れるなら `doRangeImport` で実機確認から
- `_LS_PRUNABLE` が死んでいる（v836 で発見・未着手）
- Tip入力権限の調査 / AM の管轄割り当ての実データ確認
- `supabase/functions/` が未push、`hyper-worker` が未デプロイ
- **v839 の出どころ**（並行作業のもの。`HANDOFF_v840.md` 第3章）

---

## 4. 教訓

v807 ①〜⑤、v825 ⑥〜⑫、v835 ⑬〜⑱、v836 ⑲〜㉑、v837 ㉒〜㉔、v838 ㉕㉖、v840 ㉗、v841 ㉘、v842 ㉙㉚ は有効。

### ㉛ 集計を足すときは「既存の定義と一致すること」を検証で示す

「率の全社平均を新しく作らない」という決めごとがあるため、
売上加重の平均を入れるときに **`Σ(売上×率)/Σ売上 === 総額/総売上`** を検証で毎回確かめるようにした。

**新しい式に見えるものを入れるときは、既存の定義と同じであることを機械で示す。**
文章で「同じです」と書くだけでは、あとから読む人が信用できない。

### ㉖ を仕組みで潰した（続き）

v842 で `rep()` の**置換文字列**だけ CRLF に寄せたが、**アンカー側**を忘れていて
複数行アンカーが「0件」になった。両方寄せるようにした。

```python
def rep(s, old, new, why):
    if '\r\n' in s: old, new = _crlf(old), _crlf(new)   # ← 両方
    n = s.count(old); assert n == 1, ...
    return s.replace(old, new)
```

**片側だけ直すと、別の形で同じ問題が出る。**

### ㉜ 検証の stub は本物と同じ引数を取らせる

`monthStats()` の stub が店舗IDを受け取らない形だったため、
客数・客単価が全部 `calc_error` になって落ちた。**製品ではなく検証の作りの問題。**

**stub を書くときは、本物の引数をそのまま受けて渡すこと。**
引数を捨てる stub は、店舗別・月別の違いを一切テストできない。

---

## 5. 重要な設計上の決定（勝手に変えないこと）

- **数字の計算を新しく作らない。**新しく見える集計は既存定義との一致を検証で示す（教訓㉛）
- **判定不可は 0 ではない。**`actual` は `null`、帯も付けない。
  実測は `ref`（参考値）として別に持ち「参考」と明示
- **判定できない店舗を集計に入れない。**何店ぶんかを必ず画面に出す
- **未達ゼロでも判定不可が残っていれば「達成」と言い切らない**
- **3値を扱うところで二択の `? :` を書かない**（教訓㉙）
- **ランキングを作らない。**順位番号も付けない
- **コンソールは読むだけ。**承認・タスク・エラーの状態をここで書き換えない
- 承認・タスクは**表示範囲で絞る**（AM が管轄外を見ない）
- **640px以下の既存CSSに触らない。**コンソールの CSS は `.md-*` と `body.md-wide` だけ
- **呼ばれなくなった関数は消す**

---

## 6. 同梱ファイル（追加・変更ぶん）

| ファイル | 用途 |
|---|---|
| `index.html` / `sw.js` | **v843 本体（GitHub 未 push）** |
| `preview.html` / `make_preview.js` | 画面プレビュー |
| `verify_v843.js` | **新設**。まとめ方・既存定義との一致・対象店舗数の検証（PASS=86） |
| `index_v843_backup.html` | **新設**。次版の検証が「修正前」として使う |
| `DESIGN_management_dashboard.md` | 仕様16章 → 既存コードの対応表 |
| `HANDOFF_v842.md` 〜 `HANDOFF_v807.md` | 過去の引き継ぎ |

---

## 7. 次のセッションの入り方

1. `TZ=Pacific/Honolulu node run_verify.js` → 第0章の期待値と一致するか
2. GitHub main を md5 で突き合わせる
3. `node make_preview.js` で今の画面を見る
4. **Moto さんに KPI目標と本部予算を入れてもらう**（判定不可がほどける）
5. v844（役職別の作り込み）から進める

**Motoさんは実画面を見て的確に指摘する。**作り込む前にプレビューを見てもらうこと。

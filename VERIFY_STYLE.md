# 検証スクリプトの書き方（v941 以降の既定）

## なぜ決めたか

教訓(61)(62)(64) に **v936〜v940 の1セッションだけで6回**当たった。前セッションでも
(61) に4回続けて当たっている。「気をつける」で防げていないので、道具側の既定値を直した。

`scan_verify_style.js` で全43本を機械的に見たところ、**アンチパターンのほうが標準**だった。

```
①自前のgrab    38本
②固定長の窓    28本
③素の文字列    37本
④生の凍結比較  32本
```

これは書き手の不注意ではなく、最初のテンプレートがそうなっていたのが全部に伝播した結果。

---

## 使うもの

新しい検証は `vlib.js` を使う。`require('./vlib')` した時点で、
`scan_verify_style.js` が違反を **★FAIL** にする（＝移行したら守る約束になる）。

```js
const V = require('./vlib');
const src = V.src(), prev = V.bk('939');

V.title('v941: 何をした版か');
V.version(941);                      // APP_VERSION / SW_BUILD を「以上」で見る（教訓(66)）

V.head('① 何を見るか');
V.ok(cond, 'ラベル', 実際の値);
V.okP(prev, cond, 'ラベル');          // 前版が無ければ ⏭
V.same(src, prev, 'someFn');          // 凍結
process.exit(V.report());
```

---

## 3つの落とし穴と、対応する道具

### 教訓(61) 素の文字列を数えると、自分が書いたコメントも数に入る

```js
✗ ok(!/renderMeetingMinutes/.test(rb), '呼んでいない')
    → ビルドで書いた「★関数(renderMeetingMinutes)は残す」というコメントを拾って落ちる

○ V.ok(!V.hasCall(rb, 'renderMeetingMinutes'), '呼び出しが無い')
○ V.ok(!V.has(rb, /setBudgetTab\('minutes'\)/), 'タブが無い')
```

`V.has()` `V.count()` は**先にコメントを落としてから**見る。
識別子の有無は `V.hasCall()` で**呼び出しの形**を見る。

数えるときは構文の形まで指定する。`'phase-tabs'` ではなく `class="phase-tabs"`。

### 教訓(62) 関数を取り出すと「後ろ」のコメントが付いてくる

関数の間に新しい関数を挿入すると、**直前の関数が「変わった」ことになる。**
v937・v939 でそれぞれ踏んだ。

```js
✗ okP(grab(src,'hqSetupSave') === grab(prev,'hqSetupSave'), '触っていない')
○ V.same(src, prev, 'hqSetupSave')
```

`V.fn()` が既定で末尾のコメント塊を落とす。後ろのコメントごと見たいときだけ `V.raw()`。

### 教訓(64) 固定長の窓は隣の版が挿入するとずれる

```js
✗ const seg = mv.slice(i - 200, j);
    → 隣のセルの開始タグを巻き込んで div の開閉が合わなくなる

○ const seg = V.between(body, '開始の文字列', '終了の文字列');
○ const cell = V.untilNext(body, "+'<div style=\"text-align:center\">", from);
```

`V.between()` は**見つからなければ例外**にする。黙って空文字を返して
「主張が全部通った」ように見えるのが一番わるい。

---

## 凍結の再ベース

後の版がその関数を正当に変えたら、**その版のビルドと比べる形**に直す。
最新版と比べているのが原因なので、対象を変えるだけでよい。

```js
✗ V.same(src, prev, 'renderBudget')            // src は最新版
○ V.same(V.bk('927'), prev, 'renderBudget')    // 「v927 が触っていない」が主張の意味
```

**一括置換をしない**（教訓(53)）。1本ずつ、落ちた理由を確かめてから直す。
`_bkV` を各ファイルに足す必要はもう無い。`V.bk()` を使う。

---

## 逆向きの検証

「落ちるべきときに落ちる」を確かめる。検証が甘くなっていないかを見るため。

```js
V.negative(() => V.same(変えたもの, 元, 'someFn'), '★本体が変われば落ちる');
```

中の失敗は集計に入らない。`V.negative(` を含む行は
`scan_verify_style.js` の対象外（わざとアンチパターンを書く場所なので）。

---

## 移行のしかた

**既存42本を一斉に書き換えない。** 凍結の意味が変わるものが混ざっているし、
一度に触ると何が壊れたか分からなくなる。

- 新しい検証は最初から `vlib` で書く
- 既存は、その版を再ベースする用事ができた回に1本ずつ移す
- 移行の見本は `verify_v940.js`

`run_all.sh` に `verify_vlib`（道具自身の検証・48件）と
`scan_verify_style`（書き方の点検）を登録済み。

---

## この回で index.html は変えていない

検証まわりの整備だけなので **APP_VERSION / SW_BUILD は 940 のまま**。
中身が変わっていないのに版を上げると、意味の無いキャッシュ更新が全端末で走る。

```
index.html  md5 660422707ef59b81112f5ccb8b206310   （v940 と同一）
sw.js       md5 211fdc8e27fb25bb3934224986f8d5c9   （v940 と同一）
```

# v737 — メニュータブの出数が反映されない：原因と修正

## 1. まず設計について（ご認識どおりです）

**端末は Toast に毎回つなぎに行っていません。** 流れはこうなっています。

```
Toast API
   ↓  夜間 cronSync（Edge Function）／店舗別の手動同期
Supabase の toast_item_sales テーブル
   ↓  端末はここを読むだけ（REST 1回）
端末のローカル保存（menu_mix_<店>_<期>）
   ↓
メニュータブに表示
```

`fetchToastItemSales()` は `toast_item_sales` テーブルを月指定で読むだけで、
Toast API は叩きません。**重い処理は夜間のcronだけ**です。

過去月は一度読み込んだら凍結フラグでスキップし、再取得もしません。

## 2. 反映されなかった原因

出数をレシピに紐づける処理が、**その店舗専用のレシピしか見ていませんでした**。

```
if(r.toastGuid && r.storeId===storeId) byGuid[r.toastGuid]=r.id;
                  ^^^^^^^^^^^^^^^^^^^ 完全一致のみ
```

Funergy+ のレシピは3種類あります。

| storeId | 意味 | 従来の出数反映 |
|---|---|---|
| `F04-K` など | その店舗専用 | ✅ 反映される |
| `'TOTOYA'` | **Totoya 3店の共通** | ❌ **反映されない** |
| `null` | 全店共通（旧データ含む） | ❌ **反映されない** |

すぐ下の売上合計は `recipeInStore()`（共通レシピも対象に含む正しい判定）を使っており、
**判定が食い違っていました**。そのため「クラウドにはデータがあるのに、画面の出数が0のまま」
という状態になっていました。

さらに、データが無い場合も**黙って終了**していたため、同期しても何も起きないように見えていました。

## 3. 修正

**① 判定を `recipeInStore()` に揃えた**
店舗専用を優先し、共通レシピは同じGUIDが未使用のときだけ拾います（二重計上しません）。

**② 結果を必ず知らせる**
- `○商品の出数を反映しました／未紐づけ ○件`
- `クラウドにこの月の出数がありません（夜間同期または店舗別のToast同期が必要）`
- `クラウドに○件ありましたが、メニューと紐づく商品がありませんでした`

**③ メニュータブに「出数を同期」ボタンを追加**
押すとクラウドから読み直し、結果が出ます（Toastには行きません）。

**④ 「出数の診断」も同じ判定に統一**
診断だけ厳しくて「未紐づけ」と出る状態を解消しました。

## 4. 変更ファイル
- `index.html` : APP_VERSION 736 → **737**
- `sw.js` : SW_BUILD 736 → **737**

DB変更なし。

## 5. テスト
`verify_v737.js` — **18/18 PASS**

- 出数はSupabaseのテーブルから読む（Toast APIを直接叩かない）
- **Totoya共通・全店共通のレシピに出数が付く**（従来は0のまま）
- 店舗専用を共通より優先し、二重計上しない
- 他ブランド（ToriTon）にTotoya共通は適用されない
- 日別に複数行で返っても合計する
- データが無いときは理由を返す（黙って終わらない）
- 画面側の配線3項目

回帰：v736 22/22、v735 40/40、v734 31/31、v732 予算 70/70、v728診断 0件、
v727 95/95、v719/720/721 = 21/26/33。

## 6. 確認していただきたいこと

1. v737 に更新（更新バナー → SW_BUILD 737）
2. 食材管理 ＞ メニュー を開き、店舗と月を選ぶ
3. 「**出数を同期**」を押す → `○商品の出数を反映しました` と出るか
4. 出ない場合はメッセージで原因が分かります
   - 「クラウドにこの月の出数がありません」→ 夜間同期がまだ／その月のデータが無い
   - 「メニューと紐づく商品がありませんでした」→ メニュー同期でToast商品を取り込む必要あり
5. 「出数の診断」で未紐づけの商品名も確認できます

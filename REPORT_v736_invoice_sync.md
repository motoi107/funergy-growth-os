# v736 — PCで分類した請求書が携帯に反映されない：原因と修正

## 1. 原因（コードで再現しました）

請求書を端末間で突き合わせるとき、**どちらが新しいかを「確認日時（reviewedAt）」だけで判断**していました。

```
function _invMut(x){ return x.reviewedAt ? Date.parse(x.reviewedAt) : 0; }
```

用途（食材/食材以外）の変更には時刻が付かないため、**PCも携帯も 0 で同点**になります。
同点のときの決着は「**自分の端末側を採用**」なので、こうなります。

| 段階 | 結果 |
|---|---|
| PC → クラウド | PCが「自分側」なので**変更は乗る** ✅ |
| クラウド → 携帯 | 携帯が「自分側」なので**携帯の古い方が勝つ** ❌ |

スクリーンショットの「未送信 1件」も残っていましたが、**仮に再送しても、この仕組みのままでは
携帯に反映されません**。再現テストでも、修正前の条件では携帯側の用途が空のままになりました。

## 2. 修正

**① どの編集でも押される更新時刻を追加**

```
_invMut = max( _mut（今回追加）, reviewedAt, purposeAt )
```

確認日時しか持たない**既存データとも比較できます**（Date.parse で同じ土俵に乗せます）。

**② 請求書を編集する場所すべてで時刻を押す**

- 用途の一括分類（今回反映されなかった経路）
- 確認済み／差し戻し／未確認に戻す／レビュー状態の変更
- Drive移動・画像の差し替え
- 削除（論理削除）

**③ 削除の扱いは従来どおり**

削除は時刻に関係なく優先されるので、**消したものが復活することはありません**。

## 3. 変更ファイル
- `index.html` : APP_VERSION 735 → **736**
- `sw.js` : SW_BUILD 735 → **736**

DB変更なし。既存データの書き換えもありません。

## 4. テスト
`verify_v736.js` — **22/22 PASS**

「PC → クラウド → 携帯」の流れを実際のマージ関数で再現して確認しています。

- **修正前の条件では携帯に届かない**ことを再現（症状の再現）
- 修正後は**クラウドにも携帯にも反映**される／保存確認(covers)も通る
- 逆向き（携帯で変更 → PC）も同じ
- 古い時刻で新しい変更を上書きしない（順序を変えても同じ）
- 確認日時しか無い旧データとも比較でき、あとから分類した方が残る
- 削除は復活しない
- 編集する場所8か所すべてで時刻を押している

回帰：v735 40/40、v734 31/31、v732 予算 70/70、v728診断 0件、v727 95/95、v719/720/721。

## 5. 復旧の手順（お手数ですがこの順でお願いします）

1. **PC・携帯の両方を v736 に更新**（更新バナー → SW_BUILD 736）
2. PCの同期診断で「未送信」が残っていれば「**今すぐ再送**」
3. PCで **もう一度その1件を分類し直して保存**
   （v735で分類した分には時刻が付いていないため、**付け直しが必要**です）
4. 携帯で同期 → 反映されることを確認

すでに分類した件数が多い場合は、Invoice管理の「未分類」から
**「すべて食材以外」→保存**のように一括でやり直せば、まとめて時刻が付きます。

## 6. 補足：なぜ「未送信1件」が出ていたか

保存は端末にできていて、クラウドへの送信だけが待ち状態でした。
データが失われるものではなく、オンライン時の再送で解消します。
ただし今回は**再送しても携帯側で古い方が勝っていた**ため、上記④の修正が必要でした。

# v732 — 承認月の紐づけを「承認日」で確定＋誤固定の救済

## 1. ご確認いただいた事実

**7/5 に承認したのは 7月の予算** — 確定しました。
`period` が `2026.6` なのは「アプリを使い始めたのが6月で、レコードを作った月がそのまま残っている」ため。
つまり **`period` は承認した月を表していません**。

## 2. 判定順を変更しました（v732）

旧ステータスがどの月のものかを、この順で判定します。

| 順 | 根拠 | 理由 |
|---|---|---|
| **①** | **承認日・提出日**（例 `2026/07/05 14:10` → `2026-07`） | いつ承認・提出したかを示す唯一の確かな記録 |
| ② | 手入力の月データのうち一番新しい月 | 日付が残っていない場合の代替 |
| ③ | `period` | 作成時の値。使い始めが遅いとズレるため最後 |

v731 では②を①より先に見ていました。この順だと、**8月の月データができた後に判定すると
「一番新しい月＝8月」となり、7月の承認を8月と誤認**します。テストで検出したため入れ替えました。

## 3. 誤って固定された月の救済（v732）

もし店舗端末が **v730 のまま8月を保存していた**場合、7月の数字が「6月」として固定されている
可能性があります。この場合、v731 の補正だけでは直りません。

そこで、

- 自動で固定した月には**印（`_pinned`）を付ける**ようにしました
- 補正時に、印の付いた月は**中身ごと正しい月へ移動**します（承認済み・承認者・数字を維持）
- **手入力された月データには絶対に触れません**（印が無いものは動かさない）
- 移動先にすでにデータがある場合も動かしません

## 4. 変更ファイル
- `index.html` : APP_VERSION 731 → **732**
- `sw.js` : SW_BUILD 731 → **732**

DB変更なし。

## 5. テスト
`verify_budget_v732.js` — **70/70 PASS**

v732で追加した検証：
- v730で6月に固定された状態から、**7月へ中身ごと移動**（数字25も承認者も維持、6月は空、8月は無傷）
- **印の無い（手入力の）月データは動かさない**／移動先にデータがあれば触らない
- 自動固定した月に印が付き、数字と承認状態が保たれる
- 8月の月データがある状態でも、承認日から正しく7月と判定される

回帰：v728診断 0件、v727 95/95、v719/720/721 = 21/26/33 すべてPASS。
全体構文チェック・改行（lone LF=0）確認済み。

## 6. 確認していただきたいこと

1. **GM端末と店舗端末の両方**を v732 に更新（更新バナー → SW_BUILD 732）
2. 7月を選んで「保存状況の確認」→ **Aiea / Piikoi が `2026-07=承認済み`** になっていること
   （6月ではなくなっていること）
3. 8月を選ぶと全店「下書き」＝入力・提出できること
4. 8月を保存したあと、7月に戻って**数字が変わっていない**こと

## 7. 残っている確認事項

**Waikiki Five Star Poke の7月が「GM承認待ち」のまま**です。
記録は 承認 00:16 → 提出 00:21 の順で、承認後に再提出されたように見えます。
7月を確定させるならGM承認をお願いします。

## 8. 予算まわりの修正まとめ
| 版 | 内容 |
|---|---|
| v728 | 重複解消／保存・提出の結果を必ず確認／重複修復／保存状況の診断画面 |
| v729 | 提出・承認・差し戻しを**月別**に（8月が編集不可だった原因） |
| v730 | 月データを持たない**過去月の保護** |
| v731 | 月の紐づけを**承認日から判定**＋誤りの補正 |
| v732 | 判定順を**日付最優先**に修正＋誤って固定された月の救済 |
